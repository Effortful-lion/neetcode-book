window.__BOOKS_DATA__=window.__BOOKS_DATA__||{};
window.__BOOKS_DATA__.specialAlgorithm={"book":{"title":"专题算法 · 算法笔记与原理","subtitle":"算法学习笔记汇总 + 核心算法原理详解 · 涵盖数据结构、算法专题与面试真题等内容","short":"专题算法","stats":[{"v":"19","k":"专题分类"},{"v":"125","k":"题目数量"},{"v":"Go","k":"代码实现"}]},"secs":[{"id":"sa-sec-0","secName":"","short":"","name":"算法笔记汇总","badge":"","desc":"部分内容由豆包生成  ","go":"","svg":"","notes":[],"probs":[]},{"id":"sa-sec-1","secName":"","short":"","name":"数组专题","badge":"","desc":"","go":"","svg":"","notes":[],"probs":["sa-二分查找","sa-移除元素","sa-有序数组的平方","sa-长度最小的子数组","sa-螺旋矩阵","sa-区间和","sa-开发商购买土地","sa-总结"]},{"id":"sa-sec-2","secName":"","short":"","name":"链表专题","badge":"","desc":"","go":"","svg":"","notes":[],"probs":["sa-移除链表元素","sa-设计一个链表\\(这里是一个双向链表\\)","sa-反转链表","sa-两两交换链表中的节点","sa-删除链表的倒数第N个节点","sa-[\\[面试题 02\\.07\\. 链表相交\\]](https://leetcode.cn/problems/intersection-of-two-linked-lists-lcci/)","sa-[\\[环形链表 II\\]](https://leetcode.cn/problems/linked-list-cycle-ii/)\\(链表环形入口点\\)"]},{"id":"sa-sec-3","secName":"","short":"","name":"哈希表专题","badge":"","desc":"","go":"","svg":"","notes":[],"probs":["sa-字母异位词","sa-两个数组的交集","sa-快乐数","sa-四数相加","sa-三数之和","sa-四数之和"]},{"id":"sa-sec-4","secName":"","short":"","name":"字符串专题","badge":"","desc":"","go":"","svg":"","notes":[],"probs":["sa-反转字符串","sa-替换数字","sa-翻转字符串里的单词","sa-右旋字符串","sa-实现 strStr\\(\\)","sa-重复的子字符串"]},{"id":"sa-sec-5","secName":"","short":"","name":"栈与队列","badge":"","desc":"","go":"","svg":"","notes":[],"probs":["sa-栈实现队列","sa-队列实现栈","sa-有效的括号","sa-滑动窗口最大值","sa-前K个高频元素","sa-扩展","sa-问题"]},{"id":"sa-sec-6","secName":"","short":"","name":"二叉树专题","badge":"","desc":"","go":"","svg":"","notes":[],"probs":["sa-二叉树前/中/后序遍历 递归版","sa-二叉树前/中/后序遍历 迭代版","sa-二叉树层序遍历","sa-反转二叉树","sa-对称二叉树","sa-\\[TODO 有待找到好方法\\]二叉树的高度、深度：（root高度 == 二叉树最大深度）","sa-完全二叉树的节点个数","sa-平衡二叉树","sa-二叉树的所有路径","sa-左叶子之和","sa-找树左下角的值","sa-路径总和","sa-从中序与后序遍历序列构造二叉树","sa-最大二叉树","sa-合并二叉树","sa-二叉搜索树中的搜索","sa-验证二叉搜索树","sa-二叉搜索树的最小绝对差","sa-二叉搜索树中的众数","sa-二叉树的最近公共祖先","sa-二叉搜索树的最近公共祖先","sa-二叉搜索树的插入操作","sa-删除二叉搜索树中的节点","sa-修剪二叉搜索树","sa-将有序数组转换为二叉搜索树","sa-把二叉搜索树转换为累加树"]},{"id":"sa-sec-7","secName":"","short":"","name":"回溯专题","badge":"","desc":"","go":"","svg":"","notes":[],"probs":["sa-模板","sa-组合问题","sa-组合总和III","sa-电话号码的字母组合","sa-组合总和","sa-组合总和II","sa-分割回文串","sa-复原IP地址","sa-子集（所有子集合）","sa-子集II","sa-\\[注意\\]break和continue一定要分清","sa-全排列","sa-全排列 II","sa-重新安排行程","sa-\\[好\\]N皇后","sa-解数独","sa-总结","sa-剪枝","sa-组合","sa-切割问题","sa-子集问题","sa-排列问题"]},{"id":"sa-sec-8","secName":"","short":"","name":"贪心专题","badge":"","desc":"","go":"","svg":"","notes":[],"probs":["sa-一般解题步骤","sa-分发饼干","sa-摆动序列","sa-最大子序和","sa-跳跃游戏","sa-跳跃游戏 II"]},{"id":"sa-sec-9","secName":"","short":"","name":"单调栈专题","badge":"","desc":"","go":"","svg":"","notes":[],"probs":["sa-怎么能想到用单调栈呢？ 什么时候用单调栈呢？","sa-每日温度","sa-下一个更大元素 I","sa-下一个更大元素II","sa-接雨水","sa-柱状图中最大的矩形","sa-总结"]},{"id":"sa-sec-10","secName":"","short":"","name":"动态规划专题","badge":"","desc":"","go":"","svg":"","notes":[],"probs":["sa-解题步骤","sa-基础问题","sa-\\[总结\\]一般动态规划问题","sa-背包问题"]},{"id":"sa-sec-11","secName":"","short":"","name":"图论专题","badge":"","desc":"","go":"","svg":"","notes":[],"probs":["sa-深度优先搜索（模板题）\\-\\- 有向无环图","sa-深度优先搜索（模板题）\\-\\- 岛屿数量","sa-广度优先搜索（模板题）\\-\\- 岛屿数量","sa-有向图是否有环（模板题）—— 有向有环/无环图","sa-岛屿的最大面积","sa-孤岛的总面积","sa-沉没孤岛","sa-水流问题","sa-建造最大岛屿","sa-岛屿的周长","sa-\\[总结TODO\\]岛屿问题"]},{"id":"sa-sec-12","secName":"","short":"","name":"图论岛屿问题","badge":"","desc":"","go":"","svg":"","notes":[],"probs":["sa-图论——岛屿问题总结"]},{"id":"sa-sec-13","secName":"","short":"","name":"排序算法","badge":"","desc":"","go":"","svg":"","notes":[],"probs":["sa-快速排序","sa-归并排序（自然递归）","sa-归并排序（非自然递归）","sa-堆排序"]},{"id":"sa-sec-14","secName":"","short":"","name":"并发专题","badge":"","desc":"","go":"","svg":"","notes":[],"probs":["sa-哲学家问题"]},{"id":"sa-sec-15","secName":"","short":"","name":"腾讯算法真题","badge":"","desc":"","go":"","svg":"","notes":[],"probs":["sa-LRU缓存","sa-合并两个有序列表","sa-合并 K 个有序列表","sa-合并两个链表","sa-LFU缓存"]},{"id":"sa-sec-16","secName":"","short":"","name":"算法原理汇总","badge":"","desc":"部分内容由豆包生成  ","go":"","svg":"","notes":[],"probs":[]},{"id":"sa-sec-17","secName":"","short":"","name":"树\\-DFS","badge":"","desc":"","go":"","svg":"","notes":["**核心特征**：先处理当前节点，再递归子树。当前节点的逻辑不依赖左/右子树的计算结果。","**适配问题**：","**创建/复制结构**：如深拷贝二叉树（先复制根，再复制左右子树）、根据遍历序列构建树（前序序列可作为“根”的标记）。","**记录路径/状态**：如打印从根到叶子的路径（先记录当前节点，再递归子树）、计算路径和（先累加当前节点值，再递归子树）。","**初始化操作**：如给节点添加父指针（先处理根的父指针，再处理子树）。","**理解性**：直观，符合“从根开始扩散”的思维习惯，代码逻辑与“操作顺序”一致，容易调试。","**核心特征**：先递归左子树，再处理当前节点，最后递归右子树。当前节点的逻辑依赖左子树的结果，但不依赖右子树。","**适配问题**：","**二叉搜索树（BST）操作**：BST的中序遍历是有序序列，适合找第k小元素、验证BST（左子树都小于根，根小于右子树）、求前驱/后继节点。","**依赖左子树状态的计算**：如按“左根右”顺序拼接字符串（左子树拼接完才能拼根）、计算节点在中序序列中的位置。","**理解性**：逻辑链条清晰（左子树→根→右子树），尤其在BST场景中，与“有序性”天然契合，容易理解。","**核心特征**：先递归左、右子树，再处理当前节点。当前节点的逻辑依赖左右子树的计算结果。","**适配问题**：","**计算树的属性**：如树的高度（依赖左右子树高度）、节点总数（依赖左右子树节点数）、最大路径和（依赖左右子树的最大路径）。","**释放资源/删除操作**：如删除二叉树（先删左右子树，再删根，避免悬空指针）。","**验证平衡性**：判断平衡二叉树（依赖左右子树的高度差）。","**理解性**：需要“先处理子树，再汇总结果”的思维，初期可能觉得绕，但在“依赖子树结果”的场景中，逻辑严谨性最高。","**优先选前序**：如果问题中“根节点的操作”是第一步（如创建、初始化），前序最直观，代码与思维顺序一致（先做根的事，再处理子树）。","**优先选中序**：涉及BST或“左→根→右”的顺序依赖（如有序序列），中序的逻辑链条最自然，不容易出错。","**优先选后序**：当问题明确需要“子树的结果”才能处理根（如计算高度、判断平衡），后序是唯一选择，虽然需要多一层“等待子树处理”的思维，但逻辑最贴合问题本质。","复制二叉树：不需要子树结果（先复制根）→ 前序。","验证BST：需要左子树都小于根 → 中序。","计算树高：需要左右子树高度 → 后序。","---"],"probs":["sa-系统"]},{"id":"sa-sec-18","secName":"","short":"","name":"KMP算法","badge":"","desc":"","go":"","svg":"","notes":[],"probs":["sa-面向问题","sa-kmp是解决什么问题的？","sa-kmp解决问题的核心作用？","sa-怎么实现kmp算法？（需要克服哪些问题）","sa-代码实现（各种子问题、扩展问题）"]}],"prob":{"sa-二分查找":{"title":"二分查找","slug":"","d":"m","secName":"sa-sec-1","oneline":"","steps":[],"code":"// 左闭右开\nfunc find2(target int, nums []int) int {\n    left, right := 0, len(nums)-1\n    for left &lt; right {\n       mid := left + (right-left)/2\n       if nums[mid] == target {\n          return mid\n       } else if nums[mid] &gt; target {\n          right = mid // mid 本身不满足并且取不到\n       }\n       if nums[mid] &lt; target {\n          left = mid + 1\n       }\n    }\n    return -1\n}\n\n// 左开右开\nfunc find3(target int, nums []int) int {\n    left, right := -1, len(nums)\n    for left &lt; right {\n       mid := left + (right-left)/2\n       if nums[mid] == target {\n          return mid\n       } else if nums[mid] &gt; target {\n          right = mid // mid 本身不满足并且取不到\n       }\n       if nums[mid] &lt; target {\n          left = mid // mid 本身不满足并且取不到\n       }\n    }\n    return -1\n}","tc":"","sc":"","extra":"","secFullName":"数组专题"},"sa-移除元素":{"title":"移除元素","slug":"","d":"m","secName":"sa-sec-1","oneline":"","steps":[],"code":"func backspaceCompare(s string, t string) bool {\n    p1, p2 := len(s)-1, len(t)-1\n    count1, count2 := 0, 0\n\n    for p1 &gt;= 0 || p2 &gt;= 0 {\n       // 处理 s 的退格，找到下一个有效字符位置\n       for p1 &gt;= 0 {\n          if s[p1] == '#' {\n             count1++\n             p1--\n          } else if count1 &gt; 0 {\n             // 有退格计数，跳过当前字符（被删除）\n             count1--\n             p1--\n          } else {\n             // 找到有效字符，退出内层循环\n             break\n          }\n       }\n\n       // 处理 t 的退格，找到下一个有效字符位置\n       for p2 &gt;= 0 {\n          if t[p2] == '#' {\n             count2++\n             p2--\n          } else if count2 &gt; 0 {\n             // 有退格计数，跳过当前字符（被删除）\n             count2--\n             p2--\n          } else {\n             // 找到有效字符，退出内层循环\n             break\n          }\n       }\n\n       // 比较当前有效字符\n       // 如果一个指针已越界，另一个未越界，说明不相等\n       if (p1 &gt;= 0) != (p2 &gt;= 0) {\n          return false\n       }\n       // 两个指针都未越界，但字符不同，说明不相等\n       if p1 &gt;= 0 &amp;&amp; p2 &gt;= 0 &amp;&amp; s[p1] != t[p2] {\n          return false\n       }\n\n       // 移动到下一个待处理位置\n       p1--\n       p2--\n    }\n\n    return true\n}","tc":"","sc":"","extra":"","secFullName":"数组专题"},"sa-有序数组的平方":{"title":"有序数组的平方","slug":"","d":"m","secName":"sa-sec-1","oneline":"","steps":[],"code":"func sortedSquares(nums []int) []int {\n    // 没有要求空间复杂度\n    res := make([]int, len(nums))\n    left, right := 0, len(nums)-1\n    count := len(nums) - 1\n    for left &lt; right {\n       tempLeft := nums[left] * nums[left]\n       tempRight := nums[right] * nums[right]\n       if tempLeft &gt;= tempRight {\n          res[count] = tempLeft\n          left++\n       } else {\n          res[count] = tempRight\n          right--\n       }\n       count--\n    }\n    // left == right\n    res[count] = nums[left] * nums[left]\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"数组专题"},"sa-长度最小的子数组":{"title":"长度最小的子数组","slug":"","d":"m","secName":"sa-sec-1","oneline":"","steps":[],"code":"func totalFruit(fruits []int) int {\n    // 哈希表记录当前窗口中两种水果的出现次数\n    fruitCount := make(map[int]int)\n    left := 0   // 窗口左边界\n    maxLen := 0 // 最大长度结果\n\n    // 右指针遍历所有水果\n    for right, fruit := range fruits {\n       // 将当前水果加入窗口\n       fruitCount[fruit]++\n\n       // 当窗口中水果种类超过2种时，移动左指针缩小窗口\n       for len(fruitCount) &gt; 2 {\n          leftFruit := fruits[left]\n          fruitCount[leftFruit]-- // 减少左边界水果的计数\n          if fruitCount[leftFruit] == 0 {\n             delete(fruitCount, leftFruit) // 计数为0时移除该水果\n          }\n          left++ // 左指针右移\n       }\n\n       // 更新最大长度（当前窗口长度为 right - left + 1）\n       currentLen := right - left + 1\n       if currentLen &gt; maxLen {\n          maxLen = currentLen\n       }\n    }\n\n    return maxLen\n}","tc":"","sc":"","extra":"","secFullName":"数组专题"},"sa-螺旋矩阵":{"title":"螺旋矩阵","slug":"","d":"m","secName":"sa-sec-1","oneline":"","steps":[],"code":"func generateMatrix(n int) [][]int {\n\n    if n &lt; 1{\n        return [][]int{}\n    }\n\n    // 边界\n    left, right,up,down := 0, n-1, 0, n-1\n    // 循环次数\n    loop := n/2\n    // center(奇数就是中间，偶数不需要)\n    // center := n/2\n\n    // 计数器 1 - n方\n    count := 1\n\n    // 结果数组初始化\n    res := make([][]int, n)\n    for i:=0;i&lt;n;i++{\n        res[i] = make([]int, n)\n    }\n\n    // 循环：控制边界\n    for i:=0;i&lt;loop;i++{\n        // 填充上边界\n        for col:=left;col&lt;=right;col++{\n            res[up][col] = count\n            count++\n        }\n        // 填充完成，上边界下移\n        up++\n        if up &gt; down{\n            break\n        }\n\n        // 填充右边界\n        for row:=up;row&lt;=down;row++{\n            res[row][right] = count\n            count++\n        }\n        // 填充完成，右边界左移\n        right--\n        if right &lt; left{\n            break\n        }\n\n        // 填充下边界\n        for col:=right;col&gt;=left;col--{\n            res[down][col] = count\n            count++\n        }\n        // 填充完成，下边界上移\n        down--\n        if down &lt; up{\n            break\n        }\n\n        // 填充左边界\n        for row:=down;row&gt;=up;row--{\n            res[row][left] = count\n            count++\n        }\n        // 填充完成，左边界右移\n        left++\n        if left &gt; right{\n            break\n        }\n    }\n\n    // 判断 n 是否为 奇数\n    // center(奇数就是中间，偶数不需要)\n    center := n/2\n    if n % 2 == 1{\n        // 有中间元素\n        res[center][center] = n * n // count\n    }\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"数组专题"},"sa-区间和":{"title":"区间和","slug":"","d":"m","secName":"sa-sec-1","oneline":"","steps":[],"code":"package main\n\nimport (\n    \"bufio\"\n    \"fmt\"\n    \"os\"\n    \"strconv\"\n)\n\n// 计算区间和：array 是前缀和数组\nfunc calculateRangeSum(array []int, start, end int) int {\n    // 前缀和\n    prefixSumStart := 0\n    // 如果不是第一个数，那么就初始化前缀和为：当前位置的前一个数字的前缀和\n    if start &gt; 0 {\n       prefixSumStart = array[start-1]\n    }\n    // 返回 end 的前缀和 - start 的前缀和\n    return array[end] - prefixSumStart\n}\n\nfunc main() {\n\n    // 读取性能优化 buffer scan\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // 读取数组大小\n    scanner.Scan()\n    size, _ := strconv.Atoi(scanner.Text())\n\n    // 读取数组元素\n    numberArray := make([]int, size)\n    for i := 0; i &lt; size; i++ {\n       scanner.Scan()\n       numberArray[i], _ = strconv.Atoi(scanner.Text())\n    }\n\n    // 计算前缀和数组\n    prefixArray := make([]int, size)\n    prefixArray[0] = numberArray[0]\n    for i := 1; i &lt; size; i++ {\n       prefixArray[i] = prefixArray[i-1] + numberArray[i]\n    }\n\n    // 计算区间和\n    // scan.Scan()有返回值：表示是否有输入（对应直到没输入）\n    var a, b int\n    for scanner.Scan() {\n       line := scanner.Text()\n       // 根据输入格式，从 line 中获取对应位置的数字\n       _, err := fmt.Sscanf(line, \"%d %d\", &amp;a, &amp;b)\n       if err != nil {\n          continue\n       }\n\n       if a &lt; 0 || b &gt;= size {\n          continue\n       }\n       fmt.Println(calculateRangeSum(prefixArray, a, b))\n    }\n}","tc":"","sc":"","extra":"","secFullName":"数组专题"},"sa-开发商购买土地":{"title":"开发商购买土地","slug":"","d":"m","secName":"sa-sec-1","oneline":"","steps":[],"code":"package main\n\nimport (\n    \"bufio\"\n    \"fmt\"\n    \"math\"\n    \"os\"\n    \"strconv\"\n    \"strings\"\n)\n\nfunc main() {\n    var n, m int\n    fmt.Scan(&amp;n, &amp;m)\n\n    // 初始化二维数组\n    nums := make([][]int, n)\n    for i := 0; i &lt; n; i++ {\n       nums[i] = make([]int, m)\n    }\n\n    // 读取输入（修正遍历逻辑）\n    sc := bufio.NewScanner(os.Stdin)\n    for i := 0; i &lt; n; i++ { // 正确遍历行：0~n-1\n       sc.Scan()\n       line := sc.Text()\n       parts := strings.Split(line, \" \")\n       for j := 0; j &lt; m; j++ { // 正确遍历列：0~m-1\n          nums[i][j], _ = strconv.Atoi(parts[j])\n       }\n    }\n\n    // 计算最小差值\n    fmt.Println(f(nums))\n}\n\n// f 函数：计算所有可能的单行划分或单列划分中，两部分和的最小差值\nfunc f(nums [][]int) int {\n    if len(nums) == 0 || len(nums[0]) == 0 {\n       return 0\n    }\n    n, m := len(nums), len(nums[0])\n    totalSum := 0 // 数组总和（用于快速计算划分后的另一部分和）\n\n    // 计算每行的和 与 数组总和\n    rowSums := make([]int, n)\n    for i := 0; i &lt; n; i++ {\n       for j := 0; j &lt; m; j++ {\n          rowSums[i] += nums[i][j]\n          totalSum += nums[i][j]\n       }\n    }\n\n    // 计算每列的和\n    colSums := make([]int, m)\n    for j := 0; j &lt; m; j++ {\n       for i := 0; i &lt; n; i++ {\n          colSums[j] += nums[i][j]\n       }\n    }\n\n    minDiff := math.MaxInt32 // 最小差值，初始化为最大整数\n\n    // 1. 考虑所有可能的行划分（横向切一刀，分成上下两部分）\n    // 划分位置：在第i行下方切（即上部分为0~i行，下部分为i+1~n-1行）\n    upperSum := 0              // 上部分累加和\n    for i := 0; i &lt; n-1; i++ { // 最多切n-1刀（最后一行下方无需切）\n       upperSum += rowSums[i]\n       lowerSum := totalSum - upperSum // 下部分和 = 总和 - 上部分和\n       diff := abs(upperSum - lowerSum)\n       if diff &lt; minDiff {\n          minDiff = diff\n       }\n    }\n\n    // 2. 考虑所有可能的列划分（纵向切一刀，分成左右两部分）\n    // 划分位置：在第j列右方切（即左部分为0~j列，右部分为j+1~m-1列）\n    leftSum := 0               // 左部分累加和\n    for j := 0; j &lt; m-1; j++ { // 最多切m-1刀（最后一列右方无需切）\n       leftSum += colSums[j]\n       rightSum := totalSum - leftSum // 右部分和 = 总和 - 左部分和\n       diff := abs(leftSum - rightSum)\n       if diff &lt; minDiff {\n          minDiff = diff\n       }\n    }\n\n    return minDiff\n}\n\n// 辅助函数：计算绝对值\nfunc abs(x int) int {\n    if x &lt; 0 {\n       return -x\n    }\n    return x\n}","tc":"","sc":"","extra":"","secFullName":"数组专题"},"sa-总结":{"title":"总结","slug":"","d":"m","secName":"sa-sec-9","oneline":"","steps":[],"code":"// 怎么实现单调栈？（基本思路）\n\n// eg：求右边第一个大的（递减）\n// 1. 相等，根据题意，按照逻辑进行取舍\n// 2. 遍历 &gt; 栈顶，可以进行循环判断了\n// 3. 遍历 &lt; 栈顶，直接追加栈中\n\n// 2. 遍历 &gt; 栈顶，可以进行循环判断了\nfor i := 0; i &lt; n; i++ {\n    // 1. 循环判断：当前值 &gt; 栈顶值 → 栈顶元素找到「右边第一个更大值」\n    for len(stack) &gt; 0 &amp;&amp; nums[i] &gt; nums[stack[len(stack)-1]] {\n        topIdx := stack[len(stack)-1] // 取出栈顶索引\n        stack = stack[:len(stack)-1]  // 弹出栈顶（维持单调递减）\n        res[topIdx] = nums[i]         // 记录答案：当前值是栈顶的右边第一个更大值\n    }\n    // 2. 否则（当前值 ≤ 栈顶值）→ 直接入栈（保持栈的单调递减）\n    stack = append(stack, i)\n}","tc":"","sc":"","extra":"","secFullName":"单调栈专题"},"sa-移除链表元素":{"title":"移除链表元素","slug":"","d":"m","secName":"sa-sec-2","oneline":"","steps":[],"code":"// 迭代\nfunc removeElements(head *ListNode, val int) *ListNode {\n    if head == nil{\n        return nil\n    }\n\n    dummy:=&amp;ListNode{}\n    dummy.Next = head\n\n    temp := dummy\n    for temp.Next != nil{\n        next := temp.Next\n        if next.Val == val{\n            temp.Next = next.Next\n            continue\n        }\n        temp = temp.Next\n    }\n\n    return dummy.Next\n}","tc":"","sc":"","extra":"","secFullName":"链表专题"},"sa-设计一个链表\\(这里是一个双向链表\\)":{"title":"设计一个链表\\(这里是一个双向链表\\)","slug":"","d":"m","secName":"sa-sec-2","oneline":"","steps":[],"code":"package main\n\nimport \"fmt\"\n\n// 节点结构\ntype Node struct {\n    Val  int\n    Pre  *Node // 前驱指针\n    Next *Node // 后继指针（最后一个节点为nil）\n}\n\n// 双向链表结构（带哨兵节点，非循环）\ntype MyLinkedList struct {\n    sentinel *Node // 哨兵节点（仅作为头节点的前驱）\n}\n\n// 初始化链表：哨兵节点的next指向nil（空链表）\nfunc Constructor() MyLinkedList {\n    sentinel := &amp;Node{}\n    sentinel.Next = nil // 哨兵的next初始为nil（无有效节点）\n    sentinel.Pre = nil  // 哨兵无前驱\n    return MyLinkedList{sentinel: sentinel}\n}\n\n// 获取索引为index的节点值\nfunc (this *MyLinkedList) Get(index int) int {\n    if index &lt; 0 {\n        return -1\n    }\n    // 从第一个有效节点（哨兵的next）开始遍历\n    curr := this.sentinel.Next\n    for i := 0; i &lt; index; i++ {\n        if curr == nil { // 索引超出范围\n            return -1\n        }\n        curr = curr.Next\n    }\n    if curr == nil { // 索引超出范围\n        return -1\n    }\n    return curr.Val\n}\n\n// 在头部插入节点\nfunc (this *MyLinkedList) AddAtHead(val int) {\n    newNode := &amp;Node{Val: val}\n    // 新节点的next指向原头节点（哨兵的next）\n    newNode.Next = this.sentinel.Next\n    // 新节点的pre指向哨兵\n    newNode.Pre = this.sentinel\n\n    // 哨兵的next指向新节点（更新头节点）\n    this.sentinel.Next = newNode\n\n    // 若原头节点存在，其pre指向新节点\n    if this.sentinel.Next != nil {\n        this.sentinel.Next.Pre = newNode\n    }\n}\n\n// 在尾部插入节点\nfunc (this *MyLinkedList) AddAtTail(val int) {\n    newNode := &amp;Node{Val: val}\n    newNode.Next = nil // 尾节点的next必须为nil\n\n    // 找到最后一个有效节点（next为nil的节点）\n    curr := this.sentinel\n    for curr.Next != nil {\n        curr = curr.Next\n    }\n\n    // 最后一个节点的next指向新节点\n    curr.Next = newNode\n    // 新节点的pre指向最后一个节点\n    newNode.Pre = curr\n}\n\n// 在索引index处插入节点\nfunc (this *MyLinkedList) AddAtIndex(index int, val int) {\n    if index &lt; 0 {\n        this.AddAtHead(val)\n        return\n    }\n\n    // 找到插入位置的前驱节点\n    prev := this.sentinel\n    for i := 0; i &lt; index; i++ {\n        if prev.Next == nil { // 索引超出范围, 不插入\n            return \n        }\n        prev = prev.Next\n    }\n\n    newNode := &amp;Node{Val: val}\n    // 新节点的next指向前驱的next\n    newNode.Next = prev.Next\n    // 新节点的pre指向前驱\n    newNode.Pre = prev\n\n    // 若前驱的next存在（即插入位置不是尾部），更新其pre\n    if prev.Next != nil {\n        prev.Next.Pre = newNode\n    }\n\n    // 前驱的next指向新节点\n    prev.Next = newNode\n}\n\n// 删除索引为index的节点\nfunc (this *MyLinkedList) DeleteAtIndex(index int) {\n    if index &lt; 0 {\n        return\n    }\n\n    // 找到待删除节点的前驱\n    prev := this.sentinel\n    for i := 0; i &lt; index; i++ {\n        if prev.Next == nil { // 索引超出范围\n            return\n        }\n        prev = prev.Next\n    }\n\n    delNode := prev.Next\n    if delNode == nil { // 索引超出范围\n        return\n    }\n\n    // 前驱的next指向待删除节点的next\n    prev.Next = delNode.Next\n    // 若待删除节点有后继，更新其pre\n    if delNode.Next != nil {\n        delNode.Next.Pre = prev\n    }\n}\n\n// 遍历链表（验证最后一个节点next为nil）\nfunc (this *MyLinkedList) Traverse() {\n    curr := this.sentinel.Next\n    for curr != nil {\n        fmt.Printf(\"%d \", curr.Val)\n        curr = curr.Next\n    }\n    fmt.Println()\n}","tc":"","sc":"","extra":"","secFullName":"链表专题"},"sa-反转链表":{"title":"反转链表","slug":"","d":"m","secName":"sa-sec-2","oneline":"","steps":[],"code":"/**\n * Definition for singly-linked list.\n * type ListNode struct {\n *     Val int\n *     Next *ListNode\n * }\n */\nfunc reverseList(head *ListNode) *ListNode {\n\n    if head == nil || head.Next == nil{\n        return head\n    }\n\n    cur := head\n    // 临时节点 存储\n    var pre, next *ListNode\n\n    for cur != nil{\n        // next 记录第三个\n        next = cur.Next\n        // 第二个反转\n        cur.Next = pre\n        // 操作完成后，向后推\n        pre = cur\n        cur = next\n    }\n    return pre\n}@","tc":"","sc":"","extra":"","secFullName":"链表专题"},"sa-两两交换链表中的节点":{"title":"两两交换链表中的节点","slug":"","d":"m","secName":"sa-sec-2","oneline":"","steps":[],"code":"/**\n * Definition for singly-linked list.\n * type ListNode struct {\n *     Val int\n *     Next *ListNode\n * }\n */\n\n// 递归：\nfunc swapPairs(head *ListNode) *ListNode {\n    if head == nil || head.Next == nil{\n        return head\n    }\n\n    // 发现基本相同的问题，可以想到递归\n    // 第一个\n    pre := head\n    // 第二个\n    cur := pre.Next\n    // 先递\n    // 第三个\n    node := swapPairs(cur.Next)\n    // 再归\n    // 反转\n    cur.Next = pre\n    // 连接\n    pre.Next = node\n\n    return cur\n}","tc":"","sc":"","extra":"","secFullName":"链表专题"},"sa-删除链表的倒数第N个节点":{"title":"删除链表的倒数第N个节点","slug":"","d":"m","secName":"sa-sec-2","oneline":"","steps":[],"code":"/**\n * Definition for singly-linked list.\n * type ListNode struct {\n *     Val int\n *     Next *ListNode\n * }\n */\nfunc removeNthFromEnd(head *ListNode, n int) *ListNode {\n    // 倒数第n个\n    // 计算个数\n    num := cacu(head)\n    // 示例：一共5个，删除倒数第2个，也就是正数第4个，遍历到正数第3个\n    loop := num - n\n    dummy := &amp;ListNode{Next: head}\n    temp := dummy\n    for i:=0;i&lt;loop;i++{\n        temp = temp.Next\n    }\n    del := temp.Next\n    if del != nil{\n        temp.Next = del.Next\n    }\n    return dummy.Next\n} \n\nfunc cacu(head *ListNode) int{\n    count:=0\n    for head != nil{\n        count+=1\n        head = head.Next\n    }\n    return count\n}","tc":"","sc":"","extra":"","secFullName":"链表专题"},"sa-[\\[面试题 02\\.07\\. 链表相交\\]](https://leetcode.cn/problems/intersection-of-two-linked-lists-lcci/)":{"title":"[\\[面试题 02\\.07\\. 链表相交\\]](https://leetcode.cn/problems/intersection-of-two-linked-lists-lcci/)","slug":"","d":"m","secName":"sa-sec-2","oneline":"","steps":[],"code":"func getIntersectionNode(headA, headB *ListNode) *ListNode {\n    l1,l2 := headA, headB\n    for l1 != l2 {\n        // L1 不等于 L2 就循环\n\n        // 操作L1\n        if l1 != nil {\n            l1 = l1.Next\n        } else {\n            l1 = headB\n        }\n        // 操作L2\n        if l2 != nil {\n            l2 = l2.Next\n        } else {\n            l2 = headA\n        }\n    }\n    // 找到交点了 或者 同时为nil\n    return l1\n}","tc":"","sc":"","extra":"","secFullName":"链表专题"},"sa-[\\[环形链表 II\\]](https://leetcode.cn/problems/linked-list-cycle-ii/)\\(链表环形入口点\\)":{"title":"[\\[环形链表 II\\]](https://leetcode.cn/problems/linked-list-cycle-ii/)\\(链表环形入口点\\)","slug":"","d":"m","secName":"sa-sec-2","oneline":"","steps":[],"code":"/**\n * Definition for singly-linked list.\n * type ListNode struct {\n *     Val int\n *     Next *ListNode\n * }\n */\nfunc detectCycle(head *ListNode) *ListNode {\n    // 判断是否有环，并返回入口节点\n    // 双指针 + 两次相遇 （返回原点）返回第二次相遇点\n    if head == nil || head.Next == nil{\n        return nil\n    }\n    s := head\n    q := head\n    for q != nil &amp;&amp; q.Next != nil{\n        s = s.Next\n        q = q.Next.Next\n        if s == q{\n            // 第一次相遇点\n            s = head\n            for s != q{\n                s = s.Next\n                q = q.Next\n            }\n            return s\n        }\n    }\n    return nil\n}","tc":"","sc":"","extra":"","secFullName":"链表专题"},"sa-字母异位词":{"title":"字母异位词","slug":"","d":"m","secName":"sa-sec-3","oneline":"","steps":[],"code":"func findAnagrams(s, p string) (ans []int) {\n    // 先判断长度，p是子串，一定是短或者相等\n    sLen, pLen := len(s), len(p)\n    if sLen &lt; pLen {\n        return\n    }\n\n    // 先创建和p一样长的滑动窗口（固定窗口）\n    // 创建统计表（26个字母就是26大小的数组）\n    var sCount, pCount [26]int\n    for i, ch := range p {\n        // 统计前len(p)的长度是不是\n        sCount[s[i]-'a']++\n        pCount[ch-'a']++\n    }\n    if sCount == pCount {\n        ans = append(ans, 0)\n    }\n\n    // 相当于向前推一个，维持窗口不变\n    for i, ch := range s[:sLen-pLen] {\n        sCount[ch-'a']--\n        sCount[s[i+pLen]-'a']++\n        if sCount == pCount {\n            ans = append(ans, i+1)\n        }\n    }\n    return\n}","tc":"","sc":"","extra":"","secFullName":"哈希表专题"},"sa-两个数组的交集":{"title":"两个数组的交集","slug":"","d":"m","secName":"sa-sec-3","oneline":"","steps":[],"code":"func intersect(nums1 []int, nums2 []int) []int {\n     // set 集合的交集原理:一个遍历收集，一个遍历比较\n    set := make(map[int]int)\n    for _, num := range nums1{\n        if _, ok := set[num];ok{\n            set[num]++\n        }else{\n            set[num] = 1\n        }\n    }\n    res := make(map[int]int)\n    for _, num := range nums2{\n        if _, ok := set[num]; ok{\n            // 拿一个\n            set[num]--\n            // 拿完之后没了就去掉\n            if set[num] == 0{\n                delete(set, num)\n            }\n            if _, ok := res[num];ok{\n                res[num]++\n            }else{\n                res[num] = 1\n            }\n        }\n    }\n    resNums := make([]int, 0)\n    for len(res) != 0{\n        for k, v := range res{\n            resNums = append(resNums, k)\n            // 拿走一个, 重新放回来，继续遍历\n            res[k] = v - 1\n            if res[k] == 0{\n                delete(res, k)\n            }\n        }\n    }\n    return resNums\n}","tc":"","sc":"","extra":"","secFullName":"哈希表专题"},"sa-快乐数":{"title":"快乐数","slug":"","d":"m","secName":"sa-sec-3","oneline":"","steps":[],"code":"func isHappy(n int) bool {\n    m := make(map[int]bool)for n != 1 &amp;&amp; !m[n] {\n        n, m[n] = getSum(n), true}return n == 1}func getSum(n int) int {\n    sum := 0for n &gt; 0 {\n        sum += (n % 10) * (n % 10)\n        n = n / 10}return sum\n}\n\nfunc getSum(n int) int {\n    sum := 0for n &gt; 0 {\n        sum += (n % 10) * (n % 10)\n        n = n / 10}return sum\n}","tc":"","sc":"","extra":"","secFullName":"哈希表专题"},"sa-四数相加":{"title":"四数相加","slug":"","d":"m","secName":"sa-sec-3","oneline":"","steps":[],"code":"func fourSumCount(nums1 []int, nums2 []int, nums3 []int, nums4 []int) int {  \n\n    // 这里为什么不用 set？就是可能出现重复的和 比如和为6出现4次，但是都是不同的组合\n\n    // 思路：就是 ab加和放在字典  cd加和 最后变成两数之和的思路\n\n    m := make(map[int]int)  \n    count := 0  \n\n    // 构建nums1和nums2的和的map  \n    for _, v1 := range nums1 {  \n        for _, v2 := range nums2 {  \n            m[v1+v2]++  \n        }  \n    }  \n\n    // 遍历nums3和nums4，检查-c-d是否在map中  \n    for _, v3 := range nums3 {  \n        for _, v4 := range nums4 {  \n            sum := -v3 - v4  \n            if countVal, ok := m[sum]; ok {  \n                // 出现 countVal 次\n                count += countVal  \n            }  \n        }  \n    }  \n\n    return count  \n}","tc":"","sc":"","extra":"","secFullName":"哈希表专题"},"sa-三数之和":{"title":"三数之和","slug":"","d":"m","secName":"sa-sec-3","oneline":"","steps":[],"code":"import \"sort\"\n\nfunc threeSum(nums []int) [][]int {\n    sort.Ints(nums) // 先排序,双指针去重\n    res := make([][]int, 0)\n    n := len(nums)\n\n    for i := 0; i &lt; n-2; i++ {\n        // 跳过第一个元素的重复值\n        if i &gt; 0 &amp;&amp; nums[i] == nums[i-1] {\n            continue\n        }\n        left, right := i+1, n-1\n        for left &lt; right {\n            // 这里一定是先找到再去重\n            // 去重是对已经找到的结果进行去重\n            sum := nums[i] + nums[left] + nums[right]\n            if sum == 0 {\n                res = append(res, []int{nums[i], nums[left], nums[right]})\n                // 跳过left和right的重复值\n                for left &lt; right &amp;&amp; nums[left] == nums[left+1] {\n                    left++\n                }\n                for left &lt; right &amp;&amp; nums[right] == nums[right-1] {\n                    right--\n                }\n                left++\n                right--\n            } else if sum &lt; 0 {\n                left++ // 和太小，左指针右移\n            } else {\n                right-- // 和太大，右指针左移\n            }\n        }\n    }\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"哈希表专题"},"sa-四数之和":{"title":"四数之和","slug":"","d":"m","secName":"sa-sec-3","oneline":"","steps":[],"code":"import \"sort\"\n\nfunc fourSum(nums []int, target int) [][]int {\n    // 固定一个且去重，就是三数之和\n    n := len(nums)\n    res := make([][]int, 0)\n    // 先排序，为双指针提供基础\n    sort.Ints(nums) \n    // 遍历到倒数第四个数停止\n    for i:=0;i&lt;n-3;i++{\n        // 第一个数字固定且去重\n        if i&gt;0 &amp;&amp; nums[i]==nums[i-1]{\n            continue\n        }\n        // 三数之和\n        for j:=i+1;j&lt;n-2;j++{\n            // 第二个数固定且去重（j&gt;i+1 防止 出现开端到一直结束）\n            if j&gt;i+1 &amp;&amp; nums[j]==nums[j-1]{\n                continue\n            }\n            l, r := j+1, n-1\n            for l&lt;r{\n                sum := nums[i]+nums[j]+nums[l]+nums[r]\n                if sum==target {\n                    res = append(res, []int{nums[i],nums[j],nums[l],nums[r]})\n                    // 去重，找到下一次需要判断的 l 和 r\n                    for l&lt;r &amp;&amp; nums[l] == nums[l+1]{\n                        l++\n                    }\n                    for l&lt;r &amp;&amp; nums[r] == nums[r-1]{\n                        r--\n                    }\n                    // 去重选择过的，多走一步，到未走过的\n                    l++\n                    r--\n                }else if sum&lt;target{\n                    l++\n                }else{\n                    r--\n                }\n            } \n        }\n    }\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"哈希表专题"},"sa-反转字符串":{"title":"反转字符串","slug":"","d":"m","secName":"sa-sec-4","oneline":"","steps":[],"code":"// 以 2k 为步长分割区间，多个区间的反转；上面是 0 - n-1 的一个区间反转\nfunc reverseStr(s string, k int) string {\n    sb := []byte(s) // 转换为字节切片便于修改\n    n := len(sb)\n    for i := 0; i &lt; n; i += 2 * k {\n        start := i\n        // 计算反转的结束位置：取 i+k-1 和 n-1 的最小值\n        end := i + k - 1\n        if end &gt;= n {\n            end = n - 1\n        }\n        // 反转 [start, end] 区间\n        for start &lt; end {\n            sb[start], sb[end] = sb[end], sb[start]\n            start++\n            end--\n        }\n    }\n    return string(sb)\n}","tc":"","sc":"","extra":"","secFullName":"字符串专题"},"sa-替换数字":{"title":"替换数字","slug":"","d":"m","secName":"sa-sec-4","oneline":"","steps":[],"code":"package main\n\nimport (\n        \"fmt\"\n        \"strings\"\n)\n\nfunc main() {\n        var s string\n        fmt.Scan(&amp;s)\n\n        var result strings.Builder // 用于动态构建字符串\n\n        for _, c := range s { // 遍历每个字符（rune类型，支持所有Unicode字符）\n                // 判断是否为小写字母（a-z）\n                if c &gt;= 'a' &amp;&amp; c &lt;= 'z' {\n                        result.WriteRune(c) // 保留小写字母\n                } else {\n                        result.WriteString(\"number\") // 非小写字母替换为\"number\"\n                }\n        }\n\n        fmt.Println(result.String()) // 输出最终结果\n}","tc":"","sc":"","extra":"","secFullName":"字符串专题"},"sa-翻转字符串里的单词":{"title":"翻转字符串里的单词","slug":"","d":"m","secName":"sa-sec-4","oneline":"","steps":[],"code":"// 这里直接两次反转，规律性的：局部单词反转，整体字符串反转\nimport (\n        \"strings\"\n)\n\nfunc reverseWords(s string) string {\n        // 1. 预处理：去除首尾空格，连续空格转为单个\n        // 先用Fields分割再Join，自动处理空格问题\n        words := strings.Fields(s)\n        s = strings.Join(words, \" \")\n        if s == \"\" {\n                return \"\"\n        }\n\n        // 转为字节切片便于修改\n        b := []byte(s)\n\n        // 2. 第一次反转：反转整个字符串\n        reverse(b, 0, len(b)-1)\n\n        // 3. 第二次反转：逐个反转每个单词\n        start := 0\n        for i := 0; i &lt; len(b); i++ {\n                // 遇到空格或末尾时，反转[start, i-1]区间的单词\n                if b[i] == ' ' || i == len(b)-1 {\n                        end := i\n                        if i == len(b)-1 {\n                                end = i // 最后一个单词无后续空格，end为最后一个索引\n                        } else {\n                                end = i - 1 // 非末尾单词，end为空格前一个索引\n                        }\n                        reverse(b, start, end)\n                        start = i + 1 // 下一个单词的起始位置\n                }\n        }\n\n        return string(b)\n}\n\n// 辅助函数：反转字节切片b中[left, right]区间的字符\nfunc reverse(b []byte, left, right int) {\n        for left &lt; right {\n                b[left], b[right] = b[right], b[left]\n                left++\n                right--\n        }\n}","tc":"","sc":"","extra":"","secFullName":"字符串专题"},"sa-右旋字符串":{"title":"右旋字符串","slug":"","d":"m","secName":"sa-sec-4","oneline":"","steps":[],"code":"package main\n\nimport (\n    \"fmt\"\n    \"strings\" // 导入strings包\n)\n\nfunc main() {\n    // 右旋位数\n    var k int\n    fmt.Scan(&amp;k)\n\n    // 待旋转字符串\n    var s string\n    fmt.Scan(&amp;s)\n\n    n := len(s)\n    if n == 0 { // 处理空字符串\n       fmt.Println(s)\n       return\n    }\n\n    // 优化k：当k &gt;= n时，取模避免无效旋转（如k=5, n=3，等价于右旋2位）\n    k = k % n\n    if k &lt; 0 { // 兼容负的旋转位数（左旋）\n       k += n\n    }\n\n    // 计算分割点：右旋k位，即取后k个字符 + 前n-k个字符\n    index := n - k\n    ds := s[index:] // 后k个字符\n    as := s[:index] // 前n-k个字符\n\n    // 使用strings.Builder拼接\n    var res strings.Builder\n    res.WriteString(ds) // 先写后k个字符\n    res.WriteString(as) // 再写前n-k个字符\n\n    // 输出结果\n    fmt.Println(res.String())\n}","tc":"","sc":"","extra":"","secFullName":"字符串专题"},"sa-实现 strStr\\(\\)":{"title":"实现 strStr\\(\\)","slug":"","d":"m","secName":"sa-sec-4","oneline":"","steps":[],"code":"func strStr(haystack string, needle string) int {\n    prefix := getPrefix(needle)\n    // i j 分别作为 匹配指针\n    j := 0\n    res := -1\n    for i:=0;i&lt;len(haystack);i++{ \n        for j &gt; 0 &amp;&amp; haystack[i] != needle[j] { // 匹配失败\n            j = prefix[j-1] // 回退\n        }\n        // 匹配成功\n        if haystack[i] == needle[j] {\n            j++\n            if j == len(needle) { \n                // 全部匹配成功\n                res = i - j + 1\n                break\n            }\n        }\n    }\n    return res\n}\n\nfunc getPrefix(str string) []int{ \n    prefix := make([]int, len(str))\n    if len(str) == 0 { // 处理空字符串边界 case\n        return prefix\n    }\n    prefix[0] = 0\n    // i 表示后缀结尾：既表示我们遍历/求的位置，也表示后缀结尾\n    // j 表示前缀结尾：用来和i做比较，记录前缀长度\n    // 注意： j:=0 必须要在for外面，因为j一直记录的是最长的前缀长度，不能每次都从0开始\n    j:=0\n    for i:=1;i&lt;len(str);i++{\n        // i从1开始，因为一个字符没有前后缀，已经初始化了\n        // 可能不是一步匹配\n        for j&gt;0 &amp;&amp; str[j]!=str[i]{\n            // j 需要回退：根据自身的prefix表，随时填充，随时使用的\n            j=prefix[j-1]\n        }    \n        // str[j] == str[i] || j == 0\n        if str[j] == str[i]{\n            j++\n        }\n        prefix[i] = j\n        // i++ for循环自然++\n    }\n    return prefix\n}","tc":"","sc":"","extra":"","secFullName":"字符串专题"},"sa-重复的子字符串":{"title":"重复的子字符串","slug":"","d":"m","secName":"sa-sec-4","oneline":"","steps":[],"code":"// 利用kmp算法原理，借用库函数\n// 移动匹配\n// 只要两个s拼接在一起，移除前后两个字符，如果里面还出现一个s的话，就说明是由重复子串组成。\nfunc repeatedSubstringPattern(s string) bool {\n    if len(s) == 0 {return false}\n    t := s + s\n    return strings.Contains(t[1:len(t)-1], s)\n}","tc":"","sc":"","extra":"","secFullName":"字符串专题"},"sa-栈实现队列":{"title":"栈实现队列","slug":"","d":"m","secName":"sa-sec-5","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"栈与队列"},"sa-队列实现栈":{"title":"队列实现栈","slug":"","d":"m","secName":"sa-sec-5","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"栈与队列"},"sa-有效的括号":{"title":"有效的括号","slug":"","d":"m","secName":"sa-sec-5","oneline":"","steps":[],"code":"// 先定义映射函数，用于获取右括号对应的左括号\nfunc match(ch byte) byte {\n    switch ch {\n    case ')':\n        return '('\n    case ']':\n        return '['\n    case '}':\n        return '{'\n    default:\n        return 0 // 非右括号返回0\n    }\n}\n\nfunc isValid(s string) bool {\n    stack := make([]byte, 0) // 用切片模拟栈，存储左括号\n    for i := 0; i &lt; len(s); i++ {\n        c := s[i] // 直接按byte处理（括号均为单字节ASCII字符）\n        if c == '(' || c == '[' || c == '{' {\n            // 左括号入栈\n            stack = append(stack, c)\n        } else {\n            // 右括号：检查栈是否为空或匹配\n            if len(stack) == 0 {\n                return false // 栈空却出现右括号，无效\n            }\n            // 弹出栈顶元素并比较\n            top := stack[len(stack)-1]\n            stack = stack[:len(stack)-1] // 弹出栈顶\n            if top != match(c) {\n                return false // 不匹配，无效\n            }\n        }\n    }\n    // 所有字符处理完后，栈必须为空才有效（无剩余左括号）\n    return len(stack) == 0\n}","tc":"","sc":"","extra":"","secFullName":"栈与队列"},"sa-滑动窗口最大值":{"title":"滑动窗口最大值","slug":"","d":"m","secName":"sa-sec-5","oneline":"","steps":[],"code":"func maxSlidingWindow(nums []int, k int) []int {\n    if len(nums) == 0 || k &lt;= 0 || k &gt; len(nums) {\n        return []int{}\n    }\n\n    res := make([]int, 0, len(nums)-k+1)\n    deque := make([]int, 0) // 存储索引，确保对应的值单调递减\n\n    for i := 0; i &lt; len(nums); i++ {\n        // 1. 移除队列中超出当前窗口的索引（窗口范围：[i-k+1, i]）\n        for len(deque) &gt; 0 &amp;&amp; deque[0] &lt; i-k+1 {\n            deque = deque[1:] // 队首出队\n        }\n\n        // 2. 移除队列中比当前元素小的索引（保证队列单调递减）\n        // 因为这些元素在当前元素之后，不可能成为窗口最大值\n        for len(deque) &gt; 0 &amp;&amp; nums[i] &gt;= nums[deque[len(deque)-1]] {\n            deque = deque[:len(deque)-1] // 队尾出队\n        }\n\n        // 3. 当前元素索引入队\n        deque = append(deque, i)\n\n        // 4. 当窗口形成（i &gt;= k-1），记录队首元素（当前窗口最大值）\n        if i &gt;= k-1 {\n            res = append(res, nums[deque[0]])\n        }\n    }\n\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"栈与队列"},"sa-前K个高频元素":{"title":"前K个高频元素","slug":"","d":"m","secName":"sa-sec-5","oneline":"","steps":[],"code":"//方法二:利用O(nlogn)排序\nfunc topKFrequent(nums []int, k int) []int {\n    ans:=[]int{}\n    map_num:=map[int]int{}\n    for _,item:=range nums {\n        map_num[item]++\n    }\n    for key,_:=range map_num{\n        ans=append(ans,key)\n    }\n    //核心思想：排序\n    //可以不用包函数，自己实现快排\n    sort.Slice(ans,func (a,b int)bool{\n        return map_num[ans[a]]&gt;map_num[ans[b]]\n    })\n    return ans[:k]\n}","tc":"","sc":"","extra":"","secFullName":"栈与队列"},"sa-扩展":{"title":"扩展","slug":"","d":"m","secName":"sa-sec-5","oneline":"","steps":[],"code":"// 自己实现快速排序","tc":"","sc":"","extra":"","secFullName":"栈与队列"},"sa-问题":{"title":"问题","slug":"","d":"m","secName":"sa-sec-5","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"栈与队列"},"sa-二叉树前/中/后序遍历 递归版":{"title":"二叉树前/中/后序遍历 递归版","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"// 前序遍历\nfunc PreOrder(root *TreeNode, res *[]int) {\n    // 递归函数参数：处理节点、收集数据；root、res数组\n    // 递归返回值：无\n    // 递归结束条件：遍历到当前节点为空\n    if root == nil{\n        return\n    }\n    // 递归逻辑\n    *res = append(*res, root.Val)\n    PreOrder(root.Left, res)\n    PreOrder(root.Right, res)\n    // 递归返回: 无\n}\n\n// 中序遍历\nfunc InOrder(root *TreeNode, res *[]int) {\n    // 递归函数参数：处理节点、收集数据；root、res数组\n    // 递归返回值：无\n    // 递归结束条件：遍历到当前节点为空\n    if root == nil{\n        return\n    }\n    // 递归逻辑\n    InOrder(root.Left, res)\n    *res = append(*res, root.Val)\n    InOrder(root.Right, res)\n    // 递归返回: 无\n}\n\n// 后序遍历\nfunc PostOrder(root *TreeNode, res *[]int) {\n    // 递归函数参数：处理节点、收集数据；root、res数组\n    // 递归返回值：无\n    // 递归结束条件：遍历到当前节点为空\n    if root == nil{\n        return\n    }\n    // 递归逻辑\n    PostOrder(root.Left, res)\n    PostOrder(root.Right, res)\n    *res = append(*res, root.Val)\n    // 递归返回: 无\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-二叉树前/中/后序遍历 迭代版":{"title":"二叉树前/中/后序遍历 迭代版","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"func PostOrder3(root *TreeNode, res *[]int) {\n    if root == nil {\n       return\n    }\n\n    stack := make([]*TreeNode, 0)\n    stack = append(stack, root)\n    visited := make(map[*TreeNode]bool)\n\n    for len(stack) &gt; 0 {\n       node := stack[len(stack)-1]\n       if node.Left != nil &amp;&amp; !visited[node.Left] {\n          stack = append(stack, node.Left)\n          visited[node.Left] = true\n          continue\n       }\n       if node.Right != nil &amp;&amp; !visited[node.Right] {\n          stack = append(stack, node.Right)\n          visited[node.Right] = true\n          continue\n       }\n       *res = append(*res, node.Val)\n       stack = stack[:len(stack)-1]\n    }\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-二叉树层序遍历":{"title":"二叉树层序遍历","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"func levelOrder(root *Node) [][]int {\n    if root == nil{\n        return [][]int{}\n    }\n\n    res := make([][]int, 0)\n    q := make([]*Node, 0)\n    q = append(q, root)\n\n    for len(q)&gt;0{\n        count := len(q)\n        temp := make([]int, 0)\n        for i:=0;i&lt;count;i++{\n            node := q[0]\n            q = q[1:]\n            temp = append(temp, node.Val)\n            for _, child := range node.Children{\n                if child != nil{\n                    q = append(q, child)\n                }\n            }\n        }\n        res = append(res, temp)\n    }\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-反转二叉树":{"title":"反转二叉树","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\nfunc invertTree(root *TreeNode) *TreeNode {\n    if root == nil {\n        return nil\n    }\n    if root.Left==nil&amp;&amp;root.Right==nil {\n        return root\n    }\n\n    // 注意保留副本，防止被修改\n    left := root.Left\n    right := root.Right\n    root.Right = invertTree(left)\n    root.Left = invertTree(right)\n\n    return root\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-对称二叉树":{"title":"对称二叉树","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\n // 方法二：直接 操作/比较 两个二叉树的感觉（这里）递归\nfunc isSymmetric(root *TreeNode) bool {\n    if root == nil || (root.Left == nil &amp;&amp; root.Right == nil) {\n        return true\n    }\n    // 方法一：左根右 右根左 相等\n    // 方法二：直接 操作/比较 两个二叉树的感觉（这里）\n    // 方法三：直接 反转\n    lp := root\n    rp := root\n    return compare(lp, rp)\n}\n\nfunc compare(lp *TreeNode, rp *TreeNode) bool {\n    if lp == nil || rp == nil {\n        if lp == nil &amp;&amp; rp == nil {\n            return true\n        } else {\n            return false\n        }\n    }\n    return lp.Val == rp.Val &amp;&amp; compare(lp.Left, rp.Right) &amp;&amp; compare(lp.Right, rp.Left)\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-\\[TODO 有待找到好方法\\]二叉树的高度、深度：（root高度 == 二叉树最大深度）":{"title":"\\[TODO 有待找到好方法\\]二叉树的高度、深度：（root高度 == 二叉树最大深度）","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-完全二叉树的节点个数":{"title":"完全二叉树的节点个数","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\nfunc countNodes(root *TreeNode) int {\n    // dfs    \n    return dfs(root)\n}\n\n// 递归中序\nfunc dfsInOrder(root *TreeNode) int {\n    if root == nil {\n        return 0\n    }\n\n    count := 0\n\n    // 递归处理左子树，累加左子树的节点数\n    count += dfsInOrder(root.Left)\n\n    // 处理当前节点（根）：当前节点贡献 1 个节点\n    count++\n\n    // 3. 递归处理右子树，累加右子树的节点数\n    count += dfsInOrder(root.Right)\n\n    return count\n}\n\n// 递归前序\nfunc dfsPreOrder(root *TreeNode) int {\n    if root == nil {\n        return 0\n    }\n\n    // 1. 先处理当前节点（根）：当前节点贡献 1 个节点\n    count := 1\n\n    // 2. 递归处理左子树，累加左子树的节点数\n    count += dfsPreOrder(root.Left)\n\n    // 3. 递归处理右子树，累加右子树的节点数\n    count += dfsPreOrder(root.Right)\n\n    return count\n}\n\n// 前后的区别：就是 count++ 在哪儿（前面还是后面）\n\n// 递归后序\nfunc dfs(root *TreeNode) int{\n    if root == nil{\n        return 0\n    }\n    if root.Left == nil &amp;&amp; root.Right == nil{\n        return 1\n    }\n    count := 0\n    count += dfs(root.Left)\n    count += dfs(root.Right)\n    count++\n    return count\n}\n\n// 层序\nfunc dfs2(root *TreeNode) int{\n    if root == nil{\n        return 0\n    }\n    queue := make([]*TreeNode, 0)\n    queue = append(queue, root)\n    res := 0\n    for len(queue) &gt; 0{\n        node := queue[0]\n        queue = queue[1:]\n        if node != nil{\n            queue = append(queue,node.Left)\n            queue = append(queue,node.Right)\n            res++\n        }\n    }\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-平衡二叉树":{"title":"平衡二叉树","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\nfunc isBalanced(root *TreeNode) bool {\n    if root == nil {\n        return true // 空树是平衡的\n    }\n\n    // 1. 先递归检查左、右子树是否平衡（子树内部是否所有节点都平衡）\n    leftBalanced := isBalanced(root.Left)\n    rightBalanced := isBalanced(root.Right)\n\n    // 2. 再计算当前节点的左右子树高度差\n    leftHeight := getHeight(root.Left)\n    rightHeight := getHeight(root.Right)\n    currentBalanced := abs(leftHeight-rightHeight) &lt;= 1\n\n    // 3. 三者都为true时，整树才平衡\n    return leftBalanced &amp;&amp; rightBalanced &amp;&amp; currentBalanced\n}\n\n// 单独计算子树高度的函数\nfunc getHeight(root *TreeNode) int {\n    if root == nil {\n        return 0\n    }\n    left := getHeight(root.Left)\n    right := getHeight(root.Right)\n    return max(left, right) + 1\n}\n\n// 辅助函数：计算绝对值\nfunc abs(a int) int {\n    if a &lt; 0 {\n        return -a\n    }\n    return a\n}\n\n// 辅助函数：取最大值\nfunc max(a, b int) int {\n    if a &gt; b {\n        return a\n    }\n    return b\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-二叉树的所有路径":{"title":"二叉树的所有路径","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"// 前序迭代 （这里不需要注意 根左右 对应入栈顺序是 根右左）\ntype StackElem struct {\n    Node *TreeNode\n    Path string\n}\n\nfunc binaryTreePaths(root *TreeNode) []string {\n    var res []string\n    if root == nil {\n        return res\n    }\n    stack := []StackElem{{root, fmt.Sprintf(\"%d\", root.Val)}}\n    for len(stack) &gt; 0 {\n        top := stack[len(stack)-1]\n        stack = stack[:len(stack)-1]\n        if top.Node.Left == nil &amp;&amp; top.Node.Right == nil {\n            res = append(res, top.Path)\n            continue\n        }\n        if top.Node.Right != nil {\n            stack = append(stack, StackElem{top.Node.Right, top.Path + fmt.Sprintf(\"-&gt;%d\", top.Node.Right.Val)})\n        }\n        if top.Node.Left != nil {\n            stack = append(stack, StackElem{top.Node.Left, top.Path + fmt.Sprintf(\"-&gt;%d\", top.Node.Left.Val)})\n        }\n    }\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-左叶子之和":{"title":"左叶子之和","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\nfunc sumOfLeftLeaves(root *TreeNode) int {\n    stack := make([]*TreeNode, 0)\n    stack = append(stack, root)\n    res := 0\n    for len(stack)&gt;0{\n        node := stack[len(stack)-1]\n        stack = stack[:len(stack)-1]\n\n        // 当前节点的右节点 不加 但是得访问\n        if node.Right != nil{\n            stack = append(stack, node.Right)\n        }\n        // 当前节点的左节点 是叶子节点就加 访问\n        if node.Left != nil {\n            stack = append(stack, node.Left)\n            if node.Left.Left == nil &amp;&amp; node.Left.Right == nil{\n                res += node.Left.Val\n            }\n        }\n    }\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-找树左下角的值":{"title":"找树左下角的值","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"// 这是 dfs：前序变种\nfunc findBottomLeftValue3(root *TreeNode) int {\n    var maxDepth int   // 全局最大深度\n    var leftVal int    // 最大深度对应的最左节点值\n\n    // 辅助 DFS 函数：node=当前节点，depth=当前节点深度\n    var dfs func(node *TreeNode, depth int)\n    dfs = func(node *TreeNode, depth int) {\n        if node == nil {\n            return\n        }\n\n        // 关键：先处理当前节点（叶子/非叶子都可能是最左下）\n        // 若当前深度 &gt; 最大深度：更新最大深度和节点值（左子树优先遍历，保证最左）\n        if depth &gt; maxDepth {\n            maxDepth = depth\n            leftVal = node.Val\n        }\n\n        // 优先遍历左子树（核心：同一深度下，左节点先被处理，确保最左）\n        dfs(node.Left, depth+1)\n        // 再遍历右子树\n        dfs(node.Right, depth+1)\n    }\n\n    // 根节点深度从 1 开始（或 0，只要统一即可）\n    dfs(root, 1)\n    return leftVal\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-路径总和":{"title":"路径总和","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\nfunc hasPathSum(root *TreeNode, targetSum int) bool {\n    if root == nil{\n        // if targetSum == 0{\n        //     return true\n        // }\n        // 或者全部为false（看题的意思）\n        return false\n    }\n    // 当前节点满足大小 &amp;&amp; 叶子节点 true or false\n    if targetSum - root.Val == 0{\n        if root.Left == nil &amp;&amp; root.Right == nil{\n            return true\n        }\n        // 不一定：可能叶子节点是 0 大小也满足\n        //return false\n    }\n\n    return hasPathSum(root.Left, targetSum - root.Val) || hasPathSum(root.Right, targetSum - root.Val)\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-从中序与后序遍历序列构造二叉树":{"title":"从中序与后序遍历序列构造二叉树","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\nfunc buildTree(inorder []int, postorder []int) *TreeNode {\n    // 记录索引和数值\n    im := make(map[int]int)\n    for i, v := range inorder{\n        im[v] = i\n    }\n    n := len(inorder)\n\n    // 后序数组中从后向前找第一个数字为根节点\n    // 将根节点在中序数组中寻找，找到对应的索引位置\n    // 在索引的两侧分别为左子树全部节点和右子树全部节点\n    // 左边的中序数组作为左子树的中序数组\n    // 右边的中序数组作为右子树的中序数组\n    // 具有相同数字的后序数组作为左右子树的后序数组(递归)\n    if len(postorder) == 0{\n        // 防止空数组导致下面出现数组访问越界\n        return nil\n    }\n    r := postorder[n-1]\n    rnode := Node(r)\n    index, _ := im[r]\n    leftin := inorder[:index]\n    rightin := inorder[index+1:]\n    leftpost := postorder[:index]\n    rightpost := postorder[index:n-1] \n\n    // 左子树递归\n    leftNode := buildTree(leftin, leftpost)\n    // 右子树递归\n    rightNode := buildTree(rightin, rightpost)\n    rnode.Left = leftNode\n    rnode.Right = rightNode\n    return rnode\n}\n\nfunc Node(val int) *TreeNode{\n    return &amp;TreeNode{\n        Val: val,\n    }\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-最大二叉树":{"title":"最大二叉树","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\nfunc constructMaximumBinaryTree(nums []int) *TreeNode {\n    index := getMax(nums)\n    if index == -1{\n        return nil\n    }\n    root := Node(nums[index])\n    root.Left = constructMaximumBinaryTree(nums[:index])\n    root.Right = constructMaximumBinaryTree(nums[index+1:])\n    return root\n}\n\nfunc Node(val int) *TreeNode{\n    return &amp;TreeNode{\n        Val:val,\n    }\n}\n\n// 返回最大值索引\nfunc getMax(nums []int) int{\n    max := 0\n    if len(nums) == 0{\n        return -1\n    }\n    for index, v := range nums {\n        if nums[max] &lt; v{\n            max = index\n        }\n    }\n    return max\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-合并二叉树":{"title":"合并二叉树","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\nfunc mergeTrees(root1 *TreeNode, root2 *TreeNode) *TreeNode {\n    return help(root1, root2)\n}\n\n// 方法一：直接同时操作两个树\nfunc help(root1 *TreeNode, root2 *TreeNode) *TreeNode {\n    if root1 == nil &amp;&amp; root2 == nil{\n        return nil\n    }\n    if root1 == nil &amp;&amp; root2 != nil{\n        return root2\n    }else if root1 != nil &amp;&amp; root2 == nil{\n        return root1\n    }\n    // 二者都不为nil(前序模板)\n    node := Node(root1.Val + root2.Val)\n    node.Left = help(root1.Left, root2.Left)\n    node.Right = help(root1.Right, root2.Right) \n    return node\n}\n\nfunc Node(val int) *TreeNode{\n    return &amp;TreeNode{\n        Val: val,\n    }\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-二叉搜索树中的搜索":{"title":"二叉搜索树中的搜索","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\n\n// 返回子树中的节点（问题交给子树  直接return就好了）\n// 二叉搜索树的查找：比较当前、小则左边、大则右边\nfunc searchBST(root *TreeNode, val int) *TreeNode {\n    if root == nil{\n        return nil\n    }\n    if val &lt; root.Val{\n        return searchBST(root.Left, val)\n    }else if val &gt; root.Val{\n        return searchBST(root.Right, val)\n    }\n    return root\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-验证二叉搜索树":{"title":"验证二叉搜索树","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"// 优化：\n// 1. prev 节点 优化前面的访问数组，其实访问数组就只是记录一个节点。现在只需要一个节点够了\n// 2. 中序 递归遍历：代码逻辑清晰简单\n\nfunc isValidBST(root *TreeNode) bool {\n    var help3 func(root *TreeNode) bool\n    var prev *TreeNode // 记录中序遍历的前一个节点（全局于help3）\n\n    help3 = func(root *TreeNode) bool {\n        if root == nil {\n            return true // 空节点是有效BST\n        }\n\n        // 1. 递归遍历左子树（左）\n        if !help3(root.Left) { \n            return false\n        }\n\n        // 2. 访问当前节点（根）：验证是否严格递增\n        if prev != nil &amp;&amp; root.Val &lt;= prev.Val {\n            return false\n        }\n        prev = root // 更新前一个节点\n\n        // 3. 递归遍历右子树（右）\n        return help3(root.Right) \n    }\n\n    return help3(root)\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-二叉搜索树的最小绝对差":{"title":"二叉搜索树的最小绝对差","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"func getMinimumDifference(root *TreeNode) int {\n    // 保留前一个节点的指针\n    var prev *TreeNode\n    // 定义一个比较大的值\n    min := math.MaxInt64\n    var travel func(node *TreeNode)\n    travel = func(node *TreeNode) {\n        if node == nil {\n            return \n        }\n        // 遍历左子树\n        travel(node.Left)\n        // 处理当前节点\n        if prev != nil &amp;&amp; node.Val - prev.Val &lt; min {\n            min = node.Val - prev.Val\n        }\n        // 记录前一个访问的节点\n        prev = node\n        // 遍历右子树\n        travel(node.Right)\n    }\n    travel(root)\n    return min\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-二叉搜索树中的众数":{"title":"二叉搜索树中的众数","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"func findMode(root *TreeNode) []int {\n    var res []int\n    if root == nil {\n        return res\n    }\n\n    var pre *TreeNode\n    count := 1\n    maxCount := 1\n    stack := []*TreeNode{}\n    current := root\n\n    // 迭代中序遍历：左→根→右\n    for current != nil || len(stack) &gt; 0 {\n        // 1. 遍历到左子树最深处\n        for current != nil {\n            stack = append(stack, current)\n            current = current.Left\n        }\n\n        // 2. 处理当前节点（栈顶）\n        current = stack[len(stack)-1]\n        stack = stack[:len(stack)-1]\n\n        if pre != nil {\n            if current.Val == pre.Val {\n                count++\n            } else {\n                // 结算前一个元素\n                updateRes(pre.Val, &amp;count, &amp;maxCount, &amp;res)\n                count = 1\n            }\n        }\n        pre = current\n\n        // 3. 处理右子树\n        current = current.Right\n    }\n\n    // 结算最后一个元素\n    updateRes(pre.Val, &amp;count, &amp;maxCount, &amp;res)\n\n    return res\n}\n\n// 辅助函数：根据当前计数更新结果集(这里指针为了改变上面的数字、数组)\n// 要是不清楚，可以直接替换到上面的代码中\nfunc updateRes(val int, count, maxCount *int, res *[]int) {\n    if *count &gt; *maxCount {\n        // 当前元素出现次数更多，替换结果集\n        *maxCount = *count\n        *res = []int{val}\n    } else if *count == *maxCount {\n        // 当前元素出现次数与最大值相同，追加到结果集\n        *res = append(*res, val)\n    }\n    // 次数更少则不处理\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-二叉树的最近公共祖先":{"title":"二叉树的最近公共祖先","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\nfunc lowestCommonAncestor(root, p, q *TreeNode) *TreeNode {\n    if root == nil{\n        return nil\n    }\n    // 可能根节点就是其中一个节点（root是所有节点的祖先）\n    if root.Val == p.Val || root.Val == q.Val{\n        return root\n    }\n    // 暂时不是根节点，可能在左右子树\n    left := lowestCommonAncestor(root.Left, p, q)\n    right := lowestCommonAncestor(root.Right, p, q)\n    if left != nil &amp;&amp; right != nil{\n        // 如果两边都有，说明两个节点分别在左右子树\n        // 说明root是最近的祖先\n        return root\n    }\n    // 可能有一边有\n    if left != nil{\n        return left\n    }\n    return right\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-二叉搜索树的最近公共祖先":{"title":"二叉搜索树的最近公共祖先","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val   int\n *     Left  *TreeNode\n *     Right *TreeNode\n * }\n */\n\nfunc lowestCommonAncestor(root, p, q *TreeNode) *TreeNode {\n    // 更简单，直接根据大小就可以判断元素的子树方向\n    if root == nil{\n        return nil\n    }\n\n    // 一样\n    if root.Val == p.Val || root.Val == q.Val{\n        return root\n    }\n\n    // 不一样\n    if root.Val &gt; p.Val &amp;&amp; root.Val &gt; q.Val{\n        left := lowestCommonAncestor(root.Left, p ,q)\n        if left != nil{\n            return left\n        }\n    }else if root.Val &lt; p.Val &amp;&amp; root.Val &lt; q.Val{\n        right := lowestCommonAncestor(root.Right, p ,q)\n        if right != nil{\n            return right\n        }\n    }\n\n    // 两边\n    return root\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-二叉搜索树的插入操作":{"title":"二叉搜索树的插入操作","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"// 递归\nfunc insertIntoBST(root *TreeNode, val int) *TreeNode {\n    node := &amp;TreeNode{\n        Val:val,\n    }\n    if root == nil{\n        return node\n    }\n\n    if root.Val &gt; val{\n        left := insertIntoBST(root.Left, val)\n        root.Left = left\n    }else{\n        right := insertIntoBST(root.Right, val)\n        root.Right = right\n    }\n    return root\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-删除二叉搜索树中的节点":{"title":"删除二叉搜索树中的节点","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\nfunc deleteNode(root *TreeNode, key int) *TreeNode {\n    if root == nil {\n        return nil\n    }\n    // 去右子树删除（key比当前节点大）\n    if key &gt; root.Val {\n        root.Right = deleteNode(root.Right, key)\n    } else if key &lt; root.Val { // 去左子树删除（key比当前节点小）\n        root.Left = deleteNode(root.Left, key)\n    } else { // 当前节点就是要删除的节点，分3种情况处理\n        // 情况1：无左子树，直接返回右子树（右子树为空则返回nil，等价删除）\n        if root.Left == nil {\n            return root.Right\n        }\n        // 情况2：无右子树，直接返回左子树\n        if root.Right == nil {\n            return root.Left\n        }\n        // 情况3：左右子树都存在\n        // 找右子树的最左节点（右子树的最小值节点）\n        node := root.Right\n        for node.Left != nil {\n            node = node.Left\n        }\n        // 将原节点的左子树，挂载到右子树最左节点的左子树\n        node.Left = root.Left\n        // 用右子树顶替原节点位置（原节点被删除）\n        root = root.Right\n    }\n    return root\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-修剪二叉搜索树":{"title":"修剪二叉搜索树","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"//  这里直接利用二叉搜索树的大小规律。节点比较之后就可以知道两侧的大小结果了\nfunc trimBST(root *TreeNode, low int, high int) *TreeNode {\n    if root == nil{\n        return nil\n    }\n\n    if root.Val &lt; low {\n        // 当前节点比最小值小 → 左子树全小，直接返回修剪后的右子树（右子树可能有符合条件的）\n        return trimBST(root.Right, low, high)\n    }\n    if root.Val &gt; high {\n        // 当前节点比最大值大 → 右子树全大，直接返回修剪后的左子树（左子树可能有符合条件的）\n        return trimBST(root.Left, low, high)\n    }\n\n    root.Left = trimBST(root.Left, low, high)\n    root.Right = trimBST(root.Right, low, high)\n\n    return root\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-将有序数组转换为二叉搜索树":{"title":"将有序数组转换为二叉搜索树","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\nfunc sortedArrayToBST(nums []int) *TreeNode {\n    // 要求根据有序数组恢复为二叉搜索树\n    // 1. 有序数组根本上其实可以看做是二叉搜索树的中序遍历的结果\n    // 2. 但是其实恢复一颗树需要 中序 和 至少一个前序/后序\n    // 3. 这里只有中序遍历。并且需要平衡，也就是左右子树的高度差不小于1。所以决定选择中间的作为根节点。\n    // 4. 类似二分？\n\n    // [left, right]\n    var add func(left, right int) *TreeNode\n    add = func(left, right int) *TreeNode{\n        if right &lt; left{\n            return nil\n        }\n        mid := (left + right)/2\n        root := &amp;TreeNode{\n            Val: nums[mid],\n        }\n        root.Left = add(left, mid-1)\n        root.Right = add(mid+1, right)\n        return root\n    }\n    return add(0, len(nums)-1)\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-把二叉搜索树转换为累加树":{"title":"把二叉搜索树转换为累加树","slug":"","d":"m","secName":"sa-sec-6","oneline":"","steps":[],"code":"/**\n * Definition for a binary tree node.\n * type TreeNode struct {\n *     Val int\n *     Left *TreeNode\n *     Right *TreeNode\n * }\n */\nfunc convertBST(root *TreeNode) *TreeNode {\n    // 反中序累加\n    if root == nil{\n        return nil\n    }\n\n    // 直接用一个全局的累加器，替代每次想办法读取前面的某个节点\n    sum := 0\n\n    var dfs func(root *TreeNode)\n    dfs = func(root *TreeNode){\n        if root == nil{\n            return\n        }\n        dfs(root.Right)\n        sum += root.Val\n        root.Val = sum\n        dfs(root.Left)\n        return\n    }\n    dfs(root)\n    return root\n}","tc":"","sc":"","extra":"","secFullName":"二叉树专题"},"sa-模板":{"title":"模板","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"// 复杂点：\nvoid backtracking(参数) {\n    if (终止条件) {\n        存放结果;\n        return;\n    }\n\n    for (选择：本层集合中元素（树中节点孩子的数量就是集合的大小）) {\n        处理节点;\n        backtracking(路径，选择列表); // 递归\n        回溯，撤销处理结果\n    }\n}\n\nfunc backtracking(参数) {\n    if 终止条件 {\n        存放结果\n        return\n    }\n\n    for (选择：本层集合中元素（树中节点孩子的数量就是集合的大小）) {\n        剪枝 // 可选择\n        处理节点\n        backtracking(路径，选择列表) // 递归\n        回溯，撤销处理结果\n    }\n}","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-组合问题":{"title":"组合问题","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"// 剪枝具象化：由于是组合问题，当我们遇到 12 21 这种就需要剪枝，避免出现 21 的情况。\n// 比如这里的算法是：\nif n - i + 1 &lt; k - len(path) { \n    break\n}\n\n// 翻译一下\nif 有的元素个数 &lt; 需要的长度{\n    break\n}\n\n// 细说:\n1. k 总目标的个数\n2. len(path) 目前的个数\n3. k-len(path) 需要的个数\n4. n-i+1 目前[i,n]区间有的个数\n需要 有的个数 &gt;= 需要的个数 才可以继续向下走","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-组合总和III":{"title":"组合总和III","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"func combinationSum3(k int, n int) [][]int {\n    // 两个要求:\n    // 1. 加和 == n (相比单纯组合问题,多了一个组合条件)\n    // 2. 个数 == k\n    path, res := make([]int, 0), make([][]int, 0)\n    sum := 0\n\n    var dfs func(start int)\n    dfs = func(start int){\n    // 长度到了 判断+收集\n        if len(path) == k{\n            if sum == n{\n                temp := make([]int, k)\n                copy(temp, path)\n                res = append(res, temp)\n            }\n            return  \n        }\n        for i:=start;i&lt;=9;i++{\n            // 剪枝: 长度 + 和\n            if n-i+1 &lt; k-len(path) || sum &gt;= n{\n                break\n            }\n            // 拼接\n            sum += i\n            path = append(path, i)\n            // 向下递归\n            dfs(i+1)\n            // 回溯(拼接什么,回溯什么/加了什么,回溯什么)\n            path = path[:len(path)-1]\n            sum -= i\n        }\n        return\n    }\n    dfs(1)\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-电话号码的字母组合":{"title":"电话号码的字母组合","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"import \"fmt\"\n\nfunc letterCombinations(digits string) []string {\n    // 边界条件：空字符串直接返回空\n    if digits == \"\" {\n        return []string{}\n    }\n\n    path, res := \"\", make([]string, 0)\n    // 数字到字母的映射（0/1无对应字母，直接留空）\n    cm := map[int]string{\n        0: \"\",\n        1: \"\",\n        2: \"abc\",\n        3: \"def\",\n        4: \"ghi\",\n        5: \"jkl\",\n        6: \"mno\",\n        7: \"pqrs\",\n        8: \"tuv\",\n        9: \"wxyz\",\n    }\n\n    // 转换digits为数字数组（只允许2-9）\n    nums, err := getNums(digits)\n    if err != nil {\n        return []string{}\n    }\n\n    var dfs func(index int)\n    dfs = func(index int) {\n        // 终止条件：路径长度 == 数字长度（所有数字都选完字母）\n        if len(path) == len(nums) {\n            res = append(res, path)\n            return\n        }\n\n        // 当前数字对应的字母集合\n        number := nums[index]\n        chs := cm[number]\n        // 遍历当前数字的所有字母（横向遍历）\n        for _, ch := range chs {\n            // 选择当前字母\n            path = fmt.Sprintf(\"%s%c\", path, ch)\n            // 递归处理下一个数字（纵向遍历）\n            dfs(index + 1)\n            // 回溯：撤销当前选择（关键！否则path会一直累加）\n            if len(path) &gt; 0 {\n                path = path[:len(path)-1]\n            }\n        }\n    }\n\n    dfs(0)\n    return res\n}\n\n// getNums：将digits字符串转为数字数组（仅允许2-9）\nfunc getNums(s string) ([]int, error) {\n    nums := make([]int, 0, len(s))\n    for _, c := range s {\n        // 只允许数字2-9，0/1/非数字均视为无效\n        if c &lt; '2' || c &gt; '9' {\n            return nil, fmt.Errorf(\"invalid character: %c\", c)\n        }\n        nums = append(nums, int(c-'0'))\n    }\n    return nums, nil\n}","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-组合总和":{"title":"组合总和","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"func combinationSum(candidates []int, target int) [][]int {\n    path, res := make([]int, 0), make([][]int, 0)\n    sum := 0\n\n    var dfs func(startIndex int)\n    dfs = func(startIndex int) {\n        // 终止条件：sum超过target也终止（剪枝）\n        if sum &gt; target {\n            return\n        }\n        // 找到目标和，加入结果集\n        if sum == target {\n            temp := make([]int, len(path)) // 按path长度初始化，避免copy无效\n            copy(temp, path)\n            res = append(res, temp)\n            return\n        }\n\n        // 横向遍历：从startIndex开始，避免重复组合（如[2,3]和[3,2]）\n        for i := startIndex; i &lt; len(candidates); i++ {\n            // 剪枝：当前sum+候选值&gt;target，后续候选值更大（需先排序，下面说明），直接break(可选)\n            if sum+candidates[i] &gt; target {\n                break\n            }\n            // 剪单个值,作用不大(跳过某个值)(可选)\n            // if sum+candidates[i] &gt; target {\n            //     continue\n            // }\n            // 选择当前候选值\n            sum += candidates[i]\n            path = append(path, candidates[i])\n            // 递归：允许重复选当前值（所以传i，不是i+1！）\n            dfs(i)\n            // 回溯：撤销选择（sum必须同步回溯，否则累加错误）\n            path = path[:len(path)-1]\n            sum -= candidates[i]\n        }\n    }\n\n    // 关键：排序后才能让剪枝（sum+candidates[i]&gt;target时break）生效(可选)\n    sort.Ints(candidates)\n    dfs(0)\n\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-组合总和II":{"title":"组合总和II","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"func combinationSum2(candidates []int, target int) [][]int {\n    path, res := make([]int, 0), make([][]int, 0)\n    sum := 0\n\n    var dfs func(startIndex int)\n    dfs = func(startIndex int) {\n        // 终止条件：sum超过target也终止（剪枝）\n        if sum &gt; target {\n            return\n        }\n        // 找到目标和，加入结果集\n        if sum == target {\n            temp := make([]int, len(path)) // 按path长度初始化，避免copy无效\n            copy(temp, path)\n            res = append(res, temp)\n            return\n        }\n\n        // 横向遍历：从startIndex开始，避免重复组合（如[2,3]和[3,2]）\n        for i := startIndex; i &lt; len(candidates); i++ {\n            // 剪枝1：当前sum+候选值&gt;target，后续候选值更大（需先排序，下面说明），直接break(可选)\n            if sum+candidates[i] &gt; target {\n                break\n            }\n            // 剪枝2: 去掉重复数字开头\n            if i &gt; startIndex &amp;&amp; candidates[i] == candidates[i-1]{\n                continue\n            }\n            // 选择当前候选值\n            sum += candidates[i]\n            path = append(path, candidates[i])\n            // 递归：不允许重复选当前值\n            dfs(i+1)\n            // 回溯：撤销选择（sum必须同步回溯，否则累加错误）\n            path = path[:len(path)-1]\n            sum -= candidates[i]\n        }\n    }\n\n    // (必要)\n    // 1. 关键：排序后才能让剪枝（sum+candidates[i]&gt;target时break）生效\n    // 2. 关键: 排序后同一层才能去重\n    sort.Ints(candidates)\n    dfs(0)\n\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-分割回文串":{"title":"分割回文串","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"func partition(s string) [][]string {\n    path, res := make([]string, 0), make([][]string, 0)\n    n := len(s)\n\n    // dfs 函数：从 index 开始分割字符串\n    var dfs func(index int)\n    dfs = func(index int) {\n        // 终止条件：index 到达字符串末尾，说明分割完成，收集结果\n        if index == n {\n            // 复制 path，避免后续修改影响结果\n            temp := make([]string, len(path))\n            copy(temp, path)\n            res = append(res, temp)\n            return\n        }\n\n        // 横向遍历：枚举所有以 index 为起点的子串（index ~ i）\n        for i := index; i &lt; n; i++ {\n            // 剪枝：如果当前子串不是回文，直接跳过（无需递归）\n            if !help(s, index, i) {\n                continue\n            }\n            // 选择当前子串\n            path = append(path, s[index:i+1])\n            // 递归：下一次分割从 i+1 开始\n            dfs(i + 1)\n            // 回溯：撤销选择\n            path = path[:len(path)-1]\n        }\n    }\n\n    dfs(0)\n    return res\n}\n\nfunc help(s string, left, right int) bool {\n    for left &lt; right {\n        if s[left] != s[right] {\n            return false\n        }\n        left++\n        right--\n    }\n    return true\n}","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-复原IP地址":{"title":"复原IP地址","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"// 这里需要控制目标集合的长度：&lt;=3 ，index+\n for length := 1; length &lt;= 3; length++ {\n            // 剪枝1：超出字符串长度，直接终止（后续长度更长，无需枚举）\n            if index+length &gt; n {\n                break\n            }\n            // 截取当前段\n            seg := s[index : index+length]\n            // 剪枝2：段不合法，跳过\n            if !isValidSegment(seg) {\n                continue\n            }\n\n            // 选择：加入当前段\n            segments = append(segments, seg)\n            // 递归：下一段从 index+length 开始\n            dfs(index + length)\n            // 回溯：撤销选择（删除最后一段）\n            segments = segments[:len(segments)-1]\n        }","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-子集（所有子集合）":{"title":"子集（所有子集合）","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"func subsets(nums []int) [][]int {\n    path, res := make([]int,0), make([][]int,0)\n    res = append(res, []int{})  // 任何集合的子集合都有空集\n\n    var dfs func(startIndex int)\n    dfs = func(startIndex int){\n        // 终止\n        // if startIndex == len(nums){\n        //     // 不再是收集结果，每一个节点都要收集，不只是叶子节点\n        //     return\n        // } // 发现 == len(nums) 本来就进不去for循环 省略\n        // 处理\n        for i:=startIndex;i&lt;len(nums);i++{\n            // 不需要剪枝，每个节点都要\n            // 处理：添加路径\n            path = append(path, nums[i])\n            // 每次收集路径结果\n            tmp := make([]int, len(path))\n            copy(tmp, path)\n            res = append(res, tmp)\n            // 递归 集合中的元素不重复\n            dfs(i+1)\n            // 回溯\n            path = path[:len(path)-1]\n        }\n    }\n\n    dfs(0)\n\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-子集II":{"title":"子集II","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"func subsetsWithDup(nums []int) [][]int {\n    // 先排序，为了后面的去重\n    sort.Slice(nums, func(i, j int)bool{return nums[i]&lt;nums[j]})\n\n    path, res := make([]int,0), make([][]int,0)\n    res = append(res, []int{})\n    if len(nums) == 0{\n        return res\n    }\n\n    var dfs func(index int)\n    dfs = func(index int){\n        // 同样每个节点都要收集，不需要终止\n        for i:=index;i&lt;len(nums);i++{\n            // 去重逻辑: 重复跳过（从开始的第二个字符开始检查）\n            if i&gt;index &amp;&amp; nums[i] == nums[i-1]{\n                // 这里跳过是因为后面可能还有数字没轮到呢\n                continue // 剪枝就break（跳过才continue）\n            }\n\n            // 处理\n            path = append(path, nums[i])\n            temp := make([]int, len(path))\n            copy(temp, path)\n            res = append(res, temp)\n            // 递归\n            dfs(i+1)\n            // 回溯\n            path = path[:len(path)-1]\n        }\n    }\n\n    dfs(0)\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-\\[注意\\]break和continue一定要分清":{"title":"\\[注意\\]break和continue一定要分清","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"// 递归回溯 + 非递减条件剪枝跳过\n\nfunc findSubsequences(nums []int) [][]int {\n    path, res := make([]int,0), make([][]int,0)\n\n    var dfs func(index int)\n    dfs = func(index int){\n        // 不需要终止条件，需要收集路径上的每一个结果\n        for i:=index;i&lt;len(nums);i++{\n            // 树层去重(这里不可以这样去重：题目没说一定相同元素是相邻的)\n            flag := false // 假设不重复\n            for j := index; j &lt; i; j++ {\n                // 从 index 开始检查到当前元素，如果出现重复的就跳过\n                if nums[j] == nums[i] {\n                    flag = true\n                    break\n                }\n            }\n            // 有重复的直接跳过(也就是选第一个开始)\n            if flag {\n                continue\n            }\n            // 树枝（跳过）剪枝\n            if len(path)&gt;0 &amp;&amp; nums[i]&lt;path[len(path)-1]{\n                //break  剪了nums[i]以及之后的所有结果\n                continue    // 剪了nums[i] \n            } \n            // 处理\n            path = append(path, nums[i])\n            temp := make([]int, len(path))\n            copy(temp, path)\n            // 收集条件 len(path) &gt;= 2\n            if len(path) &gt;= 2 {\n                res = append(res, temp)\n            }\n            // 递归\n            dfs(i+1)\n            // 回溯\n            path = path[:len(path)-1]\n        }\n    }\n\n    dfs(0)\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-全排列":{"title":"全排列","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"func permute(nums []int) [][]int {\n    var res [][]int          // 结果集\n    n := len(nums)\n    used := make([]bool, n)  // 标记元素是否已使用\n    path := make([]int, 0, n) // 记录当前路径\n\n    // 回溯函数\n    var backtrack func()\n    backtrack = func() {\n        // 终止条件：路径长度等于数组长度，收集结果\n        if len(path) == n {\n            // 复制 path（切片是引用类型，需深拷贝避免后续修改影响结果）\n            temp := make([]int, n)\n            copy(temp, path)\n            res = append(res, temp)\n            return\n        }\n\n        // 横向遍历：枚举所有未使用的元素\n        for i := 0; i &lt; n; i++ {\n            // 跳过已使用的元素（剪枝）\n            if used[i] {\n                continue \n            }\n\n            // 选择：标记元素为已使用，加入路径\n            used[i] = true\n            path = append(path, nums[i])\n\n            // 递归：继续向下搜索下一个元素\n            backtrack()\n\n            // 回溯：撤销选择（移除路径最后一个元素，标记为未使用）\n            path = path[:len(path)-1]\n            used[i] = false\n        }\n    }\n\n    backtrack()\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-全排列 II":{"title":"全排列 II","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"import \"sort\"\n\nfunc permuteUnique(nums []int) [][]int {\n    path, res := make([]int, 0), make([][]int, 0)\n    used := make([]bool, len(nums))\n    // 关键：先排序，让相同元素相邻\n    sort.Ints(nums)\n\n    var dfs func()\n    dfs = func() {\n        if len(path) == len(nums) {\n            temp := make([]int, len(path))\n            copy(temp, path)\n            res = append(res, temp)\n            return\n        }\n\n        for i := 0; i &lt; len(nums); i++ {\n            // 剪枝1：跳过已使用的元素\n            if used[i] {\n                continue\n            }\n            // 剪枝2：跳过同一层的重复元素（相同元素且前一个未使用）\n            if i &gt; 0 &amp;&amp; nums[i] == nums[i-1] &amp;&amp; !used[i-1] {\n                continue\n            }\n\n            // 选择\n            path = append(path, nums[i])\n            used[i] = true\n            // 递归\n            dfs()\n            // 回溯\n            path = path[:len(path)-1]\n            used[i] = false\n        }\n    }\n\n    dfs()\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-重新安排行程":{"title":"重新安排行程","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"import \"sort\"\n\nfunc findItinerary(tickets [][]string) []string {\n    // 1. 构建邻接表：from -&gt; 排序后的to列表（保证字典序）\n    adj := make(map[string][]string)\n    for _, ticket := range tickets {\n        from, to := ticket[0], ticket[1]\n        adj[from] = append(adj[from], to)\n    }\n    // 对每个from的to列表排序（确保优先选字典序小的）\n    for key := range adj {\n        sort.Strings(adj[key])\n    }\n\n    res := make([]string, 0)\n    // 2. DFS回溯找欧拉路径\n    var dfs func(from string)\n    dfs = func(from string) {\n        // 循环：当前from还有未使用的机票时，继续递归\n        for len(adj[from]) &gt; 0 {\n            // 贪心选字典序最小的to\n            to := adj[from][0]\n            // 移除这张机票（标记为已使用）\n            adj[from] = adj[from][1:]\n            // 递归访问下一个节点\n            dfs(to)\n        }\n        // 后序遍历：所有机票用完后，将当前节点加入结果\n        res = append(res, from)\n    }\n\n    dfs(\"JFK\")\n    // 3. 反转结果（后序遍历是逆序的）\n    for i, j := 0, len(res)-1; i &lt; j; i, j = i+1, j-1 {\n        res[i], res[j] = res[j], res[i]\n    }\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-\\[好\\]N皇后":{"title":"\\[好\\]N皇后","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"func solveNQueens(n int) [][]string {\n    res := make([][]string, 0)\n    if n == 0 {\n        return res\n    }\n    // 初始化棋盘：n行n列，初始值为 '.'\n    chessboard := make([]string, n)\n    for i := range chessboard {\n        row := make([]byte, n)\n        for j := range row {\n            row[j] = '.'\n        }\n        chessboard[i] = string(row)\n    }\n\n    var dfs func(row int)\n    dfs = func(row int) {\n        // 终止条件：递归到最后一行\n        if row == n {\n            // 深拷贝当前棋盘状态到结果集\n            temp := make([]string, n)\n            copy(temp, chessboard)\n            res = append(res, temp)\n            return\n        }\n\n        // 遍历当前行的每一列，尝试放置皇后\n        for col := 0; col &lt; n; col++ {\n            // 剪枝：当前位置不合法，跳过\n            if !isValid(row, col, chessboard) {\n                continue\n            }\n\n            // 选择：在当前位置放置皇后（将 string 类型转换为 byte 数组修改后再重新赋值）\n            rowBytes := []byte(chessboard[row])\n            rowBytes[col] = 'Q'\n            chessboard[row] = string(rowBytes)\n\n            // 递归：处理下一行\n            dfs(row + 1)\n\n            // 回溯：撤销选择，恢复为 '.'\n            rowBytes[col] = '.'\n            chessboard[row] = string(rowBytes)\n        }\n    }\n\n    dfs(0)\n    return res\n}\n\n// isValid 检查 (row, col) 位置放置皇后是否合法\n// 由于每一层需要检查的是已经放过的行（上面的斜线：左上和右上）\nfunc isValid(row, col int, chessboard []string) bool {\n    n := len(chessboard)\n\n    // 检查同一列是否有皇后\n    for i := 0; i &lt; row; i++ {\n        if chessboard[i][col] == 'Q' {\n            return false\n        }\n    }\n\n    // 检查左上到右下的 45 度对角线\n    for i, j := row-1, col-1; i &gt;= 0 &amp;&amp; j &gt;= 0; i, j = i-1, j-1 {\n        if chessboard[i][j] == 'Q' {\n            return false\n        }\n    }\n\n    // 检查右上到左下的 135 度对角线\n    for i, j := row-1, col+1; i &gt;= 0 &amp;&amp; j &lt; n; i, j = i-1, j+1 {\n        if chessboard[i][j] == 'Q' {\n            return false\n        }\n    }\n\n    return true\n}","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-解数独":{"title":"解数独","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"func solveSudoku(board [][]byte) {\n    var backtracking func(board [][]byte) bool\n    backtracking = func(board [][]byte) bool {\n        for i := 0; i &lt; 9; i++ {\n            for j := 0; j &lt; 9; j++ {\n                num := board[i][j]\n                //判断此位置是否适合填数字\n                if num != '.' {\n                    continue\n                }\n                //尝试填1-9\n                for k := '1'; k &lt;= '9'; k++ {\n                    if isvalid(i, j, byte(k), board) == true { //如果满足要求就填\n                        board[i][j] = byte(k)\n                        if backtracking(board) == true {\n                            return true\n                        }\n                        board[i][j] = '.'\n                    }\n                }\n                return false\n            }\n        }\n        return true\n    }\n    backtracking(board)\n}\n\n//判断填入数字是否满足要求\nfunc isvalid(row, col int, k byte, board [][]byte) bool {\n    for i := 0; i &lt; 9; i++ { //行\n        if board[row][i] == k {\n            return false\n        }\n    }\n    for i := 0; i &lt; 9; i++ { //列\n        if board[i][col] == k {\n            return false\n        }\n    }\n    //方格\n    startrow := (row / 3) * 3\n    startcol := (col / 3) * 3\n    for i := startrow; i &lt; startrow+3; i++ {\n        for j := startcol; j &lt; startcol+3; j++ {\n            if board[i][j] == k {\n                return false\n            }\n        }\n    }\n    return true\n}","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-剪枝":{"title":"剪枝","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-组合":{"title":"组合","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"// 遍历当前数字的所有字母（横向遍历）\n        for _, ch := range chs {\n            // 选择当前字母\n            path = fmt.Sprintf(\"%s%c\", path, ch)\n            // 递归处理下一个数字（纵向遍历）\n            dfs(index + 1)\n            // 回溯：撤销当前选择（关键！否则path会一直累加）\n            if len(path) &gt; 0 {\n                path = path[:len(path)-1]\n            }\n        }","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-切割问题":{"title":"切割问题","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-子集问题":{"title":"子集问题","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-排列问题":{"title":"排列问题","slug":"","d":"m","secName":"sa-sec-7","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"回溯专题"},"sa-一般解题步骤":{"title":"一般解题步骤","slug":"","d":"m","secName":"sa-sec-8","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"贪心专题"},"sa-分发饼干":{"title":"分发饼干","slug":"","d":"m","secName":"sa-sec-8","oneline":"","steps":[],"code":"func findContentChildren(g []int, s []int) int {\n    // 思路：都升序排序、之后对s进行分发\n    order(g)\n    order(s)\n    // 尽可能满足多的孩子，那就让胃口小的开始\n    gx, sx:=0, 0\n    count:=0\n    for gx &lt; len(g) &amp;&amp; sx &lt; len(s){\n        if s[sx] &gt;= g[gx]{\n            sx++\n            gx++\n            count++\n        }else{\n            // 当前饼干最小，孩子胃口也小\n            // 增大饼干\n            sx++\n        }\n    }\n    return count\n}\n\n// 提升排序为 O(nlogn)就可以了\nfunc order(nums []int) {\n    // if len(nums) == 0{\n    //     return\n    // }\n    // for i:=0;i&lt;len(nums)-1;i++{\n    //     for j:=0;j&lt;len(nums)-1-i;j++{\n    //         if nums[j]&gt;nums[j+1]{\n    //             nums[j], nums[j+1] = nums[j+1], nums[j]\n    //         }\n    //     }\n    // }\n    // return\n    sort.Ints(nums)\n}","tc":"","sc":"","extra":"","secFullName":"贪心专题"},"sa-摆动序列":{"title":"摆动序列","slug":"","d":"m","secName":"sa-sec-8","oneline":"","steps":[],"code":"func wiggleMaxLength(nums []int) int {\n    if len(nums) &lt; 2 {\n        return len(nums)\n    }\n    result := 1\n\n    // 初始不知道上下\n    up := true\n    down := true\n    // 假设先上，不符合那就假设为下，不符合那就忽略。\n    // 忽略后继续判断下一个。\n    // 上升后一定下降\n    // 下降后一定上升\n\n    for i := 1; i &lt; len(nums); i++ {\n        if up &amp;&amp; nums[i] &gt; nums[i-1] { // 上升\n            up = false\n            down = true\n            result++\n        } else if down &amp;&amp; nums[i] &lt; nums[i-1] { // 下降\n            down = false\n            up = true\n            result++\n        }\n    }\n    return result\n}","tc":"","sc":"","extra":"","secFullName":"贪心专题"},"sa-最大子序和":{"title":"最大子序和","slug":"","d":"m","secName":"sa-sec-8","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"贪心专题"},"sa-跳跃游戏":{"title":"跳跃游戏","slug":"","d":"m","secName":"sa-sec-8","oneline":"","steps":[],"code":"func canJump(nums []int) bool {\n    // 覆盖的索引范围\n    cover := 0\n    // 遍历覆盖的每个位置\n    // 不断的增大覆盖的范围\n    for i:=0;i&lt;=cover;i++{\n        num := i+nums[i]\n        if num &gt; cover{\n            cover = num\n        }\n        // 索引覆盖终点索引\n        if cover&gt;=len(nums)-1{\n            return true\n        }\n    }\n    // 返回覆盖的最后一个位置是不是大于等于终点\n    return false\n}","tc":"","sc":"","extra":"","secFullName":"贪心专题"},"sa-跳跃游戏 II":{"title":"跳跃游戏 II","slug":"","d":"m","secName":"sa-sec-8","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"贪心专题"},"sa-怎么能想到用单调栈呢？ 什么时候用单调栈呢？":{"title":"怎么能想到用单调栈呢？ 什么时候用单调栈呢？","slug":"","d":"m","secName":"sa-sec-9","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"单调栈专题"},"sa-每日温度":{"title":"每日温度","slug":"","d":"m","secName":"sa-sec-9","oneline":"","steps":[],"code":"// 暴力 差一个样例 过不了\n\nfunc dailyTemperatures(temperatures []int) []int {\n    // 暴力\n    res := make([]int, len(temperatures))\n    for i:=0;i&lt;len(temperatures);i++{\n        count := 0\n        for j:=i;j&lt;len(temperatures);j++{\n            if temperatures[j] &gt; temperatures[i]{\n                count = j-i\n                break\n            }\n        }\n        res[i] = count\n    }\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"单调栈专题"},"sa-下一个更大元素 I":{"title":"下一个更大元素 I","slug":"","d":"m","secName":"sa-sec-9","oneline":"","steps":[],"code":"func nextGreaterElement(nums1 []int, nums2 []int) []int {\n    // 定位 + 单调栈\n    // 按道理nums1中每个num都有对应的在nums2中\n    res := make([]int, len(nums1))\n\n    res_help := help(nums2)\n\n    for i, num := range nums1{\n        index := getIndex(num, nums2)\n        res[i] = res_help[index]\n    }\n    return res\n}\n\n// 找到数组中每个数右边的第一个大的数\nfunc help(nums []int) []int{\n    res := make([]int, len(nums))\n    s := make([]int, 0)\n\n    for i:=0;i&lt;len(nums);i++{\n        for len(s) &gt; 0 &amp;&amp; nums[i] &gt; nums[s[len(s)-1]]{\n            top := s[len(s)-1]\n            s = s[:len(s)-1]\n            res[top] = nums[i]\n        }\n        s = append(s, i)\n    }\n\n    for len(s)&gt;0{\n        temp := s[0]\n        s = s[1:]\n        res[temp] = -1\n    }\n    return res\n}\n\nfunc getIndex(num int, nums []int) int{\n    for i, v := range nums{\n        if num == v{\n            return i\n        }\n    }\n    return -1\n}","tc":"","sc":"","extra":"","secFullName":"单调栈专题"},"sa-下一个更大元素II":{"title":"下一个更大元素II","slug":"","d":"m","secName":"sa-sec-9","oneline":"","steps":[],"code":"func nextGreaterElements(nums []int) []int {\n    res := make([]int, len(nums))\n    s := make([]int, 0)\n    for i:=0;i&lt;len(res);i++{\n        res[i] = -1\n    }\n\n    // 比较单纯数组：多遍历一遍（这里是这个）注意越界 要取模\n    // 如果不想多想的话，直接拼成一个数组，最后截取结果\n    for i:=0;i&lt;len(nums)*2;i++{\n        for len(s) &gt; 0 &amp;&amp; nums[i%len(nums)] &gt; nums[s[len(s)-1]]{\n            top := s[len(s)-1]\n            s = s[:len(s)-1]\n            res[top] = nums[i%len(nums)]\n        }\n        s = append(s, i%len(nums))\n    }\n\n    return res\n}\n\n// 找到数组中每个数右边的第一个大的数\n// func help(nums []int) []int{\n//     res := make([]int, len(nums))\n//     s := make([]int, 0)\n\n//     for i:=0;i&lt;len(nums);i++{\n//         for len(s) &gt; 0 &amp;&amp; nums[i] &gt; nums[s[len(s)-1]]{\n//             top := s[len(s)-1]\n//             s = s[:len(s)-1]\n//             res[top] = nums[i]\n//         }\n//         s = append(s, i)\n//     }\n\n//     for len(s)&gt;0{\n//         temp := s[0]\n//         s = s[1:]\n//         res[temp] = -1\n//     }\n//     return res\n// }","tc":"","sc":"","extra":"","secFullName":"单调栈专题"},"sa-接雨水":{"title":"接雨水","slug":"","d":"m","secName":"sa-sec-9","oneline":"","steps":[],"code":"// 暴力解法\n\nfunc trap(height []int) int {\n    // 暴力：每一列的雨水之和(p = min(l, r) - curHight)\n    res := 0\n    var curHight, l, r int\n    for i:=0;i&lt;len(height);i++{\n        if i == 0 || i == len(height)-1{\n            continue\n        }\n        curHight = height[i]\n        // 找左边最大\n        l, r = curHight, curHight\n        for j:=i-1;j&gt;=0;j--{\n            if height[j] &gt; l{\n                l = height[j]\n            }\n        }\n        // 找右边最大\n        for j:=i+1;j&lt;len(height);j++{\n            if height[j] &gt; r{\n                r = height[j]\n            }\n        }\n        // 计算每一列\n        res += min(l, r) - curHight\n    } \n    return res\n}\n\nfunc min(a, b int) int{\n    if a &gt; b{\n        return b\n    }\n    return a\n}","tc":"","sc":"","extra":"","secFullName":"单调栈专题"},"sa-柱状图中最大的矩形":{"title":"柱状图中最大的矩形","slug":"","d":"m","secName":"sa-sec-9","oneline":"","steps":[],"code":"// 优化\n\nfunc largestRectangleArea(heights []int) int {\n    size := len(heights)\n    minLeftIndex := make([]int, size)\n    minRightIndex := make([]int, size)\n\n    // 记录每个柱子 左边第一个小于该柱子的下标\n    minLeftIndex[0] = -1 // 注意这里初始化，防止下面while死循环\n    for i := 1; i &lt; size; i++ {\n        t := i - 1\n        // 这里不是用if，而是不断向左寻找的过程\n        for t &gt;= 0 &amp;&amp; heights[t] &gt;= heights[i] {\n            t = minLeftIndex[t]\n        }\n        minLeftIndex[i] = t\n    }\n    // 记录每个柱子 右边第一个小于该柱子的下标\n    minRightIndex[size - 1] = size; // 注意这里初始化，防止下面while死循环\n    for i := size - 2; i &gt;= 0; i-- {\n        t := i + 1\n        // 这里不是用if，而是不断向右寻找的过程\n        for t &lt; size &amp;&amp; heights[t] &gt;= heights[i] {\n            t = minRightIndex[t]\n        }\n        minRightIndex[i] = t\n    }\n    // 求和\n    result := 0\n    for i := 0; i &lt; size; i++ {\n        sum := heights[i] * (minRightIndex[i] - minLeftIndex[i] - 1)\n        result = max(sum, result)\n    }\n    return result\n}\n\nfunc max(x, y int) int {\n    if x &gt; y {\n        return x\n    }\n    return y\n}","tc":"","sc":"","extra":"","secFullName":"单调栈专题"},"sa-解题步骤":{"title":"解题步骤","slug":"","d":"m","secName":"sa-sec-10","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"动态规划专题"},"sa-基础问题":{"title":"基础问题","slug":"","d":"m","secName":"sa-sec-10","oneline":"","steps":[],"code":"func wordBreak(s string,wordDict []string) bool  {\n    // 整体解题思路：\n    // dp[0]空字符串为true（为了递归）\n    // dp[i] = dp[j] &amp;&amp; s[j:i] 是map字典中的单词，那么 dp[i] = true\n    wordDictSet := make(map[string]bool)\n    for _, w := range wordDict {\n        wordDictSet[w] = true\n    }\n    dp := make([]bool, len(s)+1)\n    dp[0] = true\n    for i := 1; i &lt;= len(s); i++ {\n        for j := 0; j &lt; i; j++ {\n            if dp[j] &amp;&amp; wordDictSet[s[j:i]] { \n                dp[i] = true\n                break\n            }\n        }\n    }\n    return dp[len(s)]\n}","tc":"","sc":"","extra":"","secFullName":"动态规划专题"},"sa-\\[总结\\]一般动态规划问题":{"title":"\\[总结\\]一般动态规划问题","slug":"","d":"m","secName":"sa-sec-10","oneline":"","steps":[],"code":"// max(拆一次, 拆多次)\n// max(当前拆分情况, 其他最大的拆分情况)\n拆一次：j*(i-j)\n拆多次：j*dp(i-j) （因为之后的dp还可以拆分）","tc":"","sc":"","extra":"","secFullName":"动态规划专题"},"sa-背包问题":{"title":"背包问题","slug":"","d":"m","secName":"sa-sec-10","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"动态规划专题"},"sa-深度优先搜索（模板题）\\-\\- 有向无环图":{"title":"深度优先搜索（模板题）\\-\\- 有向无环图","slug":"","d":"m","secName":"sa-sec-11","oneline":"","steps":[],"code":"package main\n\nimport \"fmt\"\n\nfunc main() {\n    var n, m int\n    fmt.Scan(&n, &m)\n\n    // 邻接矩阵存储图（1~n 节点）\n    graph := make([][]int, n+1)\n    for i := range graph {\n        graph[i] = make([]int, n+1)\n    }\n\n    // 读取边\n    for i := 0; i < m; i++ {\n        var from, to int\n        fmt.Scan(&from, &to)\n        graph[from][to] = 1 // 表示 from 到 to 有边\n    }\n\n    // 存储所有路径和当前路径\n    res := make([][]int, 0)\n    path := []int{1} // 起点 1 先加入路径\n\n    // 深度优先搜索找所有路径\n    var dfs func(x int)\n    dfs = func(x int) {\n        // 到达终点 n，记录路径\n        if x == n {\n            temp := append([]int{}, path...) // 拷贝当前路径\n            res = append(res, temp)\n            return\n        }\n\n        // 遍历所有可能的下一个节点（1~n）\n        for i := 1; i <= n; i++ {\n            // 存在 x 到 i 的边\n            if graph[x][i] == 1 {\n                // 处理：加入当前节点\n                path = append(path, i)\n                // 递归：访问下一个节点\n                dfs(i)\n                // 回溯：移除当前节点\n                path = path[:len(path)-1]\n            }\n        }\n    }\n\n    // 从起点 1 开始搜索\n    dfs(1)\n\n    // 输出结果\n    if len(res) == 0 {\n        fmt.Print(\"-1\")\n        return\n    }\n\n    for _, path := range res {\n        printPath(path)\n    }\n    return\n}\n\nfunc printPath(nums []int) {\n    for i, v := range nums {\n        if i == len(nums)-1 {\n            fmt.Printf(\"%d\", v)\n        } else {\n            fmt.Printf(\"%d \", v)\n        }\n    }\n    fmt.Println()\n}","tc":"","sc":"","extra":"","secFullName":"图论专题"},"sa-深度优先搜索（模板题）\\-\\- 岛屿数量":{"title":"深度优先搜索（模板题）\\-\\- 岛屿数量","slug":"","d":"m","secName":"sa-sec-11","oneline":"","steps":[],"code":"package main\n\nimport \"fmt\"\n\nfunc main(){\n    // 存储岛屿数据\n    var n,m int\n    fmt.Scan(&n,&m)\n    g := make([][]int,n)\n    for i := range g{\n        g[i] = make([]int, m)\n    }\n    for i:=0;i<n;i++{\n        for j:=0;j<m;j++{\n            fmt.Scan(&g[i][j])\n        }\n    }\n    // 定义访问数组\n    visit := make([][]bool, n)\n    for i:= range visit{\n        visit[i] = make([]bool, m)\n    }\n    // 定义方向数组\n    dir := [4][]int{\n        []int{0, 1},\n        []int{1, 0},\n        []int{0, -1},\n        []int{-1, 0},\n    }\n\n    // 计算岛屿的数量\n    var dfs func(x, y int)\n    dfs = func(x, y int){\n        // 终止\n        if visit[x][y] || g[x][y] == 0{\n            return\n        }\n        // 处理\n        visit[x][y] = true\n        for i:=0;i<4;i++{\n            // 确定移动方向\n            newx := x + dir[i][0]\n            newy := y + dir[i][1]\n            // 越界判断\n            if newx < 0 || newx >= len(g) || newy < 0 || newy >= len(g[0]){\n                continue\n            }\n            // 递归\n            dfs(newx, newy)\n        }\n    }\n\n    res := 0\n\n    for i:=0;i<len(g);i++{\n        for j:=0;j<len(g[0]);j++{\n            if !visit[i][j] && g[i][j] == 1{\n                // 搜索每一个 没搜索过 + 陆地 节点\n                res++\n                dfs(i, j)\n            }\n        }\n    }\n    fmt.Print(res)\n    return\n}","tc":"","sc":"","extra":"","secFullName":"图论专题"},"sa-广度优先搜索（模板题）\\-\\- 岛屿数量":{"title":"广度优先搜索（模板题）\\-\\- 岛屿数量","slug":"","d":"m","secName":"sa-sec-11","oneline":"","steps":[],"code":"package main\n\nimport (\n    \"fmt\"\n)\n\n// 定义四个方向：右、下、上、左\nvar dir = [][]int{{0, 1}, {1, 0}, {-1, 0}, {0, -1}}\n\nfunc main() {\n    // 存储岛屿数据\n    var n,m int\n    fmt.Scan(&n,&m)\n    grid := make([][]int,n)\n    for i := range grid{\n        grid[i] = make([]int, m)\n    }\n    for i:=0;i<n;i++{\n        for j:=0;j<m;j++{\n            fmt.Scan(&grid[i][j])\n        }\n    }\n\n    // 初始化访问标记数组\n    visited := make([][]bool, n)\n    for i := 0; i < n; i++ {\n        visited[i] = make([]bool, m)\n    }\n\n    // 定义 BFS 闭包：捕获外部的 grid、visited、dir 变量\n    bfs := func(x, y int) {\n        // 初始化队列，存储坐标 [x, y]\n        queue := [][]int{{x, y}}\n        visited[x][y] = true // 加入队列后立即标记已访问\n\n        // 队列不为空时循环\n        for len(queue) > 0 {\n            // 出队（取队首元素）\n            cur := queue[0]\n            queue = queue[1:] // 截取切片，模拟出队\n            curx, cury := cur[0], cur[1]\n\n            // 遍历四个方向\n            for i := 0; i < 4; i++ {\n                nextx := curx + dir[i][0]\n                nexty := cury + dir[i][1]\n\n                // 越界判断\n                if nextx < 0 || nextx >= len(grid) || nexty < 0 || nexty >= len(grid[0]) {\n                    continue\n                }\n\n                // 未访问且是陆地（值为 1），入队并标记\n                if !visited[nextx][nexty] && grid[nextx][nexty] == 1 {\n                    queue = append(queue, []int{nextx, nexty})\n                    visited[nextx][nexty] = true\n                }\n            }\n        }\n    }\n\n    // 统计岛屿数量\n    result := 0\n    for i := 0; i < n; i++ {\n        for j := 0; j < m; j++ {\n            if !visited[i][j] && grid[i][j] == 1 {\n                result++\n                bfs(i, j) // 调用闭包\n            }\n        }\n    }\n\n    // 输出结果\n    fmt.Println(result)\n}","tc":"","sc":"","extra":"","secFullName":"图论专题"},"sa-有向图是否有环（模板题）—— 有向有环/无环图":{"title":"有向图是否有环（模板题）—— 有向有环/无环图","slug":"","d":"m","secName":"sa-sec-11","oneline":"","steps":[],"code":"// 课程表：判断有向图是否存在【环】\n// 思路：拓扑排序（BFS）\nfunc canFinish(numCourses int, prerequisites [][]int) bool {\n    // ---------------------\n    // 1. 建图：有向图的【邻接表】\n    // g[i] 存储：从节点 i 出发，能到达的所有邻接节点\n    // 图论定义：有向边 u → v\n    // ---------------------\n    g := make([][]int, numCourses)\n\n    // ---------------------\n    // 2. 入度数组 in-degree\n    // d[i]：节点 i 的【入度】→ 有多少条边指向 i\n    // ---------------------\n    d := make([]int, numCourses)\n\n    // 遍历所有边，构建邻接表和入度数组\n    for _, row := range prerequisites {\n        course := row[0]       // 后继节点 v\n        precourse := row[1]     // 前驱节点 u\n        g[precourse] = append(g[precourse], course) // 添加有向边：u → v\n        d[course]++ // 节点 v 的入度 +1\n    }\n\n    // ---------------------\n    // 3. 初始化队列：放入所有【入度为 0】的节点\n    // 拓扑排序起点：入度为0的节点（没有前驱，可以直接学）\n    // ---------------------\n    queue := []int{}\n    for i, v := range d {\n        if v == 0 {\n            queue = append(queue, i)\n        }\n    }\n\n    // ---------------------\n    // 4. BFS 执行拓扑排序\n    // 每次取出【入度为0】的节点，删除它的出边\n    // ---------------------\n    count := 0 // 统计拓扑序列的节点数量\n    for len(queue) > 0 {\n        // 取出队首节点 u（当前入度为0，可处理）\n        u := queue[0]\n        queue = queue[1:]\n        count++ // 该节点加入拓扑序列\n\n        // ---------------------\n        // 遍历 u 的所有邻接节点 v（u → v）\n        // 删掉边 u→v：v 的入度 -1\n        // ---------------------\n        for _, v := range g[u] {\n            d[v]--\n            // 如果 v 的入度变为 0，加入队列\n            if d[v] == 0 {\n                queue = append(queue, v)\n            }\n        }\n    }\n\n    // ---------------------\n    // 5. 判断是否有环\n    // 拓扑序列长度 == 总节点数 → 无环 → 可以完成课程\n    // 拓扑序列长度 < 总节点数 → 有环 → 无法完成\n    // ---------------------\n    return count == numCourses\n}","tc":"","sc":"","extra":"","secFullName":"图论专题"},"sa-岛屿的最大面积":{"title":"岛屿的最大面积","slug":"","d":"m","secName":"sa-sec-11","oneline":"","steps":[],"code":"// dfs\n\npackage main\n\nimport \"fmt\"\n\nfunc main() {\n    // 输入\n    var n, m int\n    fmt.Scan(&n, &m)\n    // 初始化网格\n    g := make([][]int, n)\n    for i := range g {\n        g[i] = make([]int, m)\n    }\n    for i := 0; i < n; i++ {\n        for j := 0; j < m; j++ {\n            fmt.Scan(&g[i][j])\n        }\n    }\n\n    // 处理\n    res := 0 // 存储最大面积\n    visited := make([][]bool, n)\n    for i := range visited {\n        visited[i] = make([]bool, m)\n    }\n    // 四个方向：右、下、上、左\n    dir := [][]int{{0, 1}, {1, 0}, {-1, 0}, {0, -1}}\n\n    // 定义 DFS 闭包，返回当前连通块的面积\n    var dfs func(x, y int) int\n    dfs = func(x, y int) int {\n        // 递归终止条件：越界、已访问、不是陆地\n        if x < 0 || x >= len(g) || y < 0 || y >= len(g[0]) || visited[x][y] || g[x][y] != 1 {\n            return 0\n        }\n        // 标记当前节点为已访问\n        visited[x][y] = true\n        // 当前节点面积为 1，累加四个方向的面积\n        area := 1\n        for i := 0; i < 4; i++ {\n            nextx := x + dir[i][0]\n            nexty := y + dir[i][1]\n            area += dfs(nextx, nexty) // 递归遍历相邻节点，累加面积\n        }\n        return area // 返回当前连通块的总面积\n    }\n\n    // 遍历网格，寻找最大连通块\n    for i := 0; i < n; i++ {\n        for j := 0; j < m; j++ {\n            if g[i][j] == 1 && !visited[i][j] {\n                currentArea := dfs(i, j)\n                res = max(res, currentArea)\n            }\n        }\n    }\n\n    // 输出结果\n    fmt.Println(res)\n}\n\n// 辅助函数：返回两个整数的最大值\nfunc max(a, b int) int {\n    if a > b {\n        return a\n    }\n    return b\n}","tc":"","sc":"","extra":"","secFullName":"图论专题"},"sa-孤岛的总面积":{"title":"孤岛的总面积","slug":"","d":"m","secName":"sa-sec-11","oneline":"","steps":[],"code":"package main\n\nimport \"fmt\"\n\n// 计算每个孤岛面积\nfunc main(){\n    var n,m int\n    fmt.Scan(&n,&m)\n    g := make([][]int, n)\n    for i := range g{\n        g[i] = make([]int, m)\n    }\n    for i:=0;i<n;i++{\n        for j:=0;j<m;j++{\n            fmt.Scan(&g[i][j])\n        }\n    }\n\n    res := 0\n    visited := make([][]bool, n)\n    for i := range visited {\n        visited[i] = make([]bool, m)\n    }\n    // 四个方向：右、下、上、左\n    dir := [][]int{{0, 1}, {1, 0}, {-1, 0}, {0, -1}}\n    var flag bool\n\n    var dfs func(x, y int) int\n    dfs = func(x, y int) int{\n        // 不合法 或者 是海域 就跳过\n        if x < 0 || x >= len(g) || y < 0 || y >= len(g[0]) || visited[x][y] || g[x][y] != 1 {\n            return 0\n        }\n\n        // 判断是否为孤岛(就是判断每个单元格是不是边缘)\n        if x == 0 || y == 0 || x == len(g)-1 || y == len(g[0])-1{\n            flag = true\n        }\n\n        // 当前面积\n        area := 1\n        visited[x][y] = true\n        for i := 0; i < 4; i++ {\n            nextx := x + dir[i][0]\n            nexty := y + dir[i][1]\n            area += dfs(nextx, nexty) // 递归遍历相邻节点，累加面积\n        }\n        return area // 返回当前连通块的总面积\n    } \n\n    for i:=0;i<n;i++{\n        for j:=0;j<m;j++{\n            // 重置孤岛标志(默认是孤岛)\n            flag = false\n            // 可探索且是孤岛\n            if !visited[i][j] && g[i][j] == 1{\n                // 岛屿面积\n                temp := dfs(i, j)\n                // 是否为孤岛\n                if !flag {\n                    res += temp\n                }\n            }\n        }\n    }\n    // 输出res\n    fmt.Print(res)\n    return\n}","tc":"","sc":"","extra":"","secFullName":"图论专题"},"sa-沉没孤岛":{"title":"沉没孤岛","slug":"","d":"m","secName":"sa-sec-11","oneline":"","steps":[],"code":"package main\n\nimport \"fmt\"\n\n// 计算每个孤岛面积\nfunc main(){\n    // 输入\n    var n,m int\n    fmt.Scan(&n,&m)\n    g := make([][]int, n)\n    for i := range g{\n        g[i] = make([]int, m)\n    }\n    for i:=0;i<n;i++{\n        for j:=0;j<m;j++{\n            fmt.Scan(&g[i][j])\n        }\n    }\n    // 处理\n    visited := make([][]bool, n)\n    for i := range visited {\n        visited[i] = make([]bool, m)\n    }\n    dir := [][]int{{0, 1}, {1, 0}, {-1, 0}, {0, -1}}\n\n    // 通过dfs将沿边的陆地变成海域\n    var dfs func(x, y int)\n    dfs = func(x, y int){\n        // 不合法(越界/访问过) 或者 是海域 就跳过\n        if x < 0 || x >= len(g) || y < 0 || y >= len(g[0]) || visited[x][y] || g[x][y] != 1 {\n            return\n        }\n\n        // 处理\n        visited[x][y] = true\n        g[x][y] = 2\n        // 向四个方向去辐射\n        for i:=0;i<4;i++{\n            newx, newy := x+dir[i][0], y+dir[i][1]\n            dfs(newx, newy)\n        }\n        return\n    } \n\n    // 第一次标记(从各个边缘陆地开始)\n    for i:=0;i<n;i++{\n        if g[i][0] == 1{\n            dfs(i, 0)\n        }\n        if g[i][m-1] == 1{\n            dfs(i, m-1)\n        }\n    }\n    for i:=0;i<m;i++{\n        if g[0][i] == 1{\n            dfs(0, i)\n        }\n        if g[n-1][i] == 1{\n            dfs(n-1, i)\n        }\n    }\n    // 第二次收集(剩下的全是孤岛的陆地)\n    for i:=0;i<n;i++{\n        for j:=0;j<m;j++{\n            if g[i][j] == 1 || g[i][j] == 0{\n                g[i][j] = 0\n            }else if g[i][j] == 2{\n                // 特殊标记的变成陆地\n                g[i][j] = 1\n            }\n        }\n    }\n\n    // 输出res\n    print(g)\n    return\n}\n\nfunc print(res [][]int){\n    for i:=0;i<len(res);i++{\n        for j:=0;j<len(res[0]);j++{\n            if j == len(res[0])-1{\n                fmt.Printf(\"%d\", res[i][j])\n            }else{\n                fmt.Printf(\"%d \", res[i][j])\n            }\n        }\n        fmt.Println()\n    }\n}","tc":"","sc":"","extra":"","secFullName":"图论专题"},"sa-水流问题":{"title":"水流问题","slug":"","d":"m","secName":"sa-sec-11","oneline":"","steps":[],"code":"package main\n\nimport (\n    \"bufio\"\n    \"fmt\"\n    \"os\"\n    \"strconv\"\n    \"strings\"\n)\n\nvar directions = [][]int{{0, -1}, {0, 1}, {-1, 0}, {1, 0}} // 四个方向的偏移量\n\nfunc main() {\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // 读取行列数\n    scanner.Scan()\n    lineList := strings.Fields(scanner.Text())\n    N, _ := strconv.Atoi(lineList[0])\n    M, _ := strconv.Atoi(lineList[1])\n\n    // 读取矩阵\n    grid := make([][]int, N)\n    for i := 0; i < N; i++ {\n        grid[i] = make([]int, M)\n        scanner.Scan()\n        lineList = strings.Fields(scanner.Text())\n        for j := 0; j < M; j++ {\n            grid[i][j], _ = strconv.Atoi(lineList[j])\n        }\n    }\n\n    // 定义DFS闭包，直接访问外部的grid、directions、N、M，以及每次遍历的visited\n    var dfs func(x, y int) (bool, bool)\n    var visited [][]bool // 每次遍历新起点时重置\n\n    // 闭包实现DFS逻辑\n    dfs = func(x, y int) (bool, bool) {\n        visited[x][y] = true\n        // 第一组边界：i=0 或 j=0\n        canReachFirst := (x == 0 || y == 0)\n        // 第二组边界：i=N-1 或 j=M-1\n        canReachSecond := (x == N-1 || y == M-1)\n\n        // 遍历四个方向\n        for _, dir := range directions {\n            nextx := x + dir[0]\n            nexty := y + dir[1]\n            // 边界检查\n            if nextx < 0 || nextx >= N || nexty < 0 || nexty >= M {\n                continue\n            }\n            // 水流规则：只能流向更低或等高的位置，且未访问过\n            if grid[nextx][nexty] <= grid[x][y] && !visited[nextx][nexty] {\n                r1, r2 := dfs(nextx, nexty)\n                // 合并子节点的遍历结果\n                canReachFirst = canReachFirst || r1\n                canReachSecond = canReachSecond || r2\n            }\n        }\n        return canReachFirst, canReachSecond\n    }\n\n    // 遍历每个单元格，检查是否能同时到达两组边界\n    for i := 0; i < N; i++ {\n        for j := 0; j < M; j++ {\n            // 每次DFS前重置visited数组\n            visited = make([][]bool, N)\n            for k := range visited {\n                visited[k] = make([]bool, M)\n            }\n            canReachFirst, canReachSecond := dfs(i, j)\n            if canReachFirst && canReachSecond {\n                fmt.Printf(\"%d %d\\n\", i, j)\n            }\n        }\n    }\n}","tc":"","sc":"","extra":"","secFullName":"图论专题"},"sa-建造最大岛屿":{"title":"建造最大岛屿","slug":"","d":"m","secName":"sa-sec-11","oneline":"","steps":[],"code":"package main\n\nimport (\n    \"fmt\"\n)\n\nfunc main() {\n    var n, m int\n    // 读取地图行数n、列数m\n    fmt.Scan(&n, &m)\n    // 初始化地图\n    g := make([][]int, n)\n    for i := range g {\n        g[i] = make([]int, m)\n    }\n    // 读取地图数据（0=海水，1=陆地）\n    for i := 0; i < n; i++ {\n        for j := 0; j < m; j++ {\n            fmt.Scan(&g[i][j])\n        }\n    }\n\n    // 1. 第一步：标记所有岛屿并记录每个岛屿的面积\n    visit := make([][]bool, n)\n    for i := range visit {\n        visit[i] = make([]bool, m)\n    }\n    islandArea := make(map[int]int) // 岛屿编号 -> 面积\n    mark := 2                       // 岛屿编号从2开始（避免和0/1冲突）\n    dir := [][]int{{0, 1}, {1, 0}, {0, -1}, {-1, 0}} // 上下左右四个方向\n    isAllLand := true               // 标记是否全是陆地\n\n    // DFS函数：标记岛屿、计算面积\n    var dfs func(x, y int)\n    dfs = func(x, y int) {\n        // 边界判断：越界则返回\n        if x < 0 || x >= n || y < 0 || y >= m {\n            return\n        }\n        // 已访问 或 是海水：返回\n        if visit[x][y] || g[x][y] != 1 {\n            return\n        }\n        // 标记为已访问\n        visit[x][y] = true\n        // 标记当前格子属于哪个岛屿\n        g[x][y] = mark\n        // 岛屿面积+1（通过闭包更新）\n        islandArea[mark]++\n\n        // 遍历四个方向\n        for _, d := range dir {\n            nextX := x + d[0]\n            nextY := y + d[1]\n            dfs(nextX, nextY)\n        }\n    }\n\n    // 遍历整个地图，标记所有岛屿\n    for i := 0; i < n; i++ {\n        for j := 0; j < m; j++ {\n            // 存在海水，说明不是全陆地\n            if g[i][j] == 0 {\n                isAllLand = false\n            }\n            // 未访问的陆地：启动DFS标记岛屿\n            if !visit[i][j] && g[i][j] == 1 {\n                islandArea[mark] = 0 // 初始化当前岛屿面积\n                dfs(i, j)            // 注意：原代码传了x/y（未定义），应传i/j\n                mark++               // 下一个岛屿编号+1\n            }\n        }\n    }\n\n    // 特殊情况：全是陆地，最大面积就是地图总面积\n    if isAllLand {\n        fmt.Println(n * m)\n        return\n    }\n\n    // 2. 第二步：遍历所有海水格子，计算添加陆地后的最大面积\n    maxArea := 0\n    for i := 0; i < n; i++ {\n        for j := 0; j < m; j++ {\n            // 只处理海水格子（添加陆地的位置）\n            if g[i][j] == 0 {\n                // 记录当前格子周围的岛屿编号（去重）\n                visitedIslands := make(map[int]bool)\n                currentArea := 1 // 新增的陆地本身占1\n                // 遍历四个方向\n                for _, d := range dir {\n                    nearX := i + d[0]\n                    nearY := j + d[1]\n                    // 边界判断\n                    if nearX < 0 || nearX >= n || nearY < 0 || nearY >= m {\n                        continue\n                    }\n                    // 周围是岛屿（编号>=2）且未统计过\n                    islandID := g[nearX][nearY]\n                    if islandID >= 2 && !visitedIslands[islandID] {\n                        currentArea += islandArea[islandID]\n                        visitedIslands[islandID] = true // 标记已统计，避免重复加\n                    }\n                }\n                // 更新最大面积\n                if currentArea > maxArea {\n                    maxArea = currentArea\n                }\n            }\n        }\n    }\n\n    // 输出结果\n    fmt.Println(maxArea)\n}","tc":"","sc":"","extra":"","secFullName":"图论专题"},"sa-岛屿的周长":{"title":"岛屿的周长","slug":"","d":"m","secName":"sa-sec-11","oneline":"","steps":[],"code":"package main\n\nimport (\n        \"fmt\"\n)\n\nfunc main() {\n        var n, m int\n        fmt.Scan(&n, &m)\n        g := make([][]int, n)\n        for i := range g {\n                g[i] = make([]int, m)\n        }\n\n        landCount := 0   // 陆地总数\n        adjCount := 0    // 相邻陆地对数\n        // 读取地图时同步统计：陆地数 + 右侧/下侧相邻陆地数（避免重复统计）\n        for i := 0; i < n; i++ {\n                for j := 0; j < m; j++ {\n                        fmt.Scan(&g[i][j])\n                        if g[i][j] == 1 {\n                                landCount++\n                                // 统计右侧相邻陆地（i,j 和 i,j+1）\n                                if j+1 < m && g[i][j+1] == 1 {\n                                        adjCount++\n                                }\n                                // 统计下侧相邻陆地（i,j 和 i+1,j）\n                                if i+1 < n && g[i+1][j] == 1 {\n                                        adjCount++\n                                }\n                        }\n                }\n        }\n\n        // 总海岸线 = 总边数 - 2×相邻对数\n        res := landCount*4 - adjCount*2\n        fmt.Println(res)\n}","tc":"","sc":"","extra":"","secFullName":"图论专题"},"sa-\\[总结TODO\\]岛屿问题":{"title":"\\[总结TODO\\]岛屿问题","slug":"","d":"m","secName":"sa-sec-11","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"图论专题"},"sa-图论——岛屿问题总结":{"title":"图论——岛屿问题总结","slug":"","d":"m","secName":"sa-sec-12","oneline":"","steps":[],"code":"// DFS计算岛屿周长\nfunc islandPerimeterDFS(grid [][]int) int {\n        if len(grid) == 0 || len(grid[0]) == 0 {\n                return 0\n        }\n        n, m := len(grid), len(grid[0])\n        perimeter := 0\n\n        var dfs func(x, y int)\n        dfs = func(x, y int) {\n                if x < 0 || x >= n || y < 0 || y >= m || grid[x][y] != 1 {\n                        return\n                }\n                grid[x][y] = 0 // 标记已访问\n                // 统计当前格子的四方向周长\n                for _, d := range dirs {\n                        nx, ny := x+d[0], y+d[1]\n                        if nx < 0 || nx >= n || ny < 0 || ny >= m || grid[nx][ny] == 0 {\n                                perimeter++\n                        }\n                }\n                // 遍历四方向\n                for _, d := range dirs {\n                        dfs(x+d[0], y+d[1])\n                }\n        }\n\n        // 遍历所有格子\n        for i := 0; i < n; i++ {\n                for j := 0; j < m; j++ {\n                        if grid[i][j] == 1 {\n                                dfs(i, j)\n                        }\n                }\n        }\n        return perimeter\n}\n\n// BFS计算岛屿周长（切片队列）\nfunc islandPerimeterBFS(grid [][]int) int {\n        if len(grid) == 0 || len(grid[0]) == 0 {\n                return 0\n        }\n        n, m := len(grid), len(grid[0])\n        perimeter := 0\n\n        bfs := func(startX, startY int) {\n                queue := [][]int{{startX, startY}}\n                grid[startX][startY] = 0\n\n                for len(queue) > 0 {\n                        cur := queue[0]\n                        queue = queue[1:]\n                        x, y := cur[0], cur[1]\n\n                        // 统计当前格子的四方向周长\n                        for _, d := range dirs {\n                                nx, ny := x+d[0], y+d[1]\n                                if nx < 0 || nx >= n || ny < 0 || ny >= m || grid[nx][ny] == 0 {\n                                        perimeter++\n                                }\n                        }\n\n                        // 遍历四方向，入队未访问的陆地\n                        for _, d := range dirs {\n                                nx, ny := x+d[0], y+d[1]\n                                if nx >= 0 && nx < n && ny >= 0 && ny < m && grid[nx][ny] == 1 {\n                                        queue = append(queue, []int{nx, ny})\n                                        grid[nx][ny] = 0\n                                }\n                        }\n                }\n        }\n\n        // 遍历所有格子\n        for i := 0; i < n; i++ {\n                for j := 0; j < m; j++ {\n                        if grid[i][j] == 1 {\n                                bfs(i, j)\n                        }\n                }\n        }\n        return perimeter\n}","tc":"","sc":"","extra":"","secFullName":"图论岛屿问题"},"sa-快速排序":{"title":"快速排序","slug":"","d":"m","secName":"sa-sec-13","oneline":"","steps":[],"code":"package main\n\nimport \"fmt\"\n\nfunc main() {\n    nums := []int{4, 2, 5, 6, 2, 7}\n    sortArray(nums)\n    fmt.Println(nums)\n}\n\nfunc sortArray(nums []int) []int {\n    QUICK(nums, 0, len(nums)-1)\n    return nums\n}\n\n// 快排序\nfunc QUICK(arr []int, low, high int) {\n    if low < high { // 基线条件：子数组长度>1才需要排序（否则直接返回）\n       pivotIndex := partition(arr, low, high) // 找基准值的正确位置\n       QUICK(arr, low, pivotIndex-1)           // 递归排基准左侧（都≤基准）\n       QUICK(arr, pivotIndex+1, high)          // 递归排基准右侧（都>基准）\n    }\n}\n\nfunc partition(arr []int, low, high int) int {\n    pivot := arr[low]\n    l, r := low, high\n    for l < r {\n       // 左边：找到第一个大的\n       for l < r && arr[l] <= pivot {\n          l++\n       }\n       // 右边：找到第一个小的\n       for l < r && arr[r] >= pivot {\n          r--\n       }\n       // 交换\n       arr[l], arr[r] = arr[r], arr[l]\n    }\n    // 将基准放到中间\n    arr[low], arr[l] = arr[l], arr[low]\n    return l\n}","tc":"","sc":"","extra":"","secFullName":"排序算法"},"sa-归并排序（自然递归）":{"title":"归并排序（自然递归）","slug":"","d":"m","secName":"sa-sec-13","oneline":"","steps":[],"code":"func sortArray(nums []int) []int {\n    return sort1(nums)\n}\n\n// 改动核心：从“拆分到单个元素”改为“找有序子段+合并”\nfunc sort1(nums []int) []int {\n    if len(nums) == 0 {\n        return nums\n    }\n    // 步骤1：找出所有天然有序的子段（run）\n    runs := findRuns(nums)\n    // 步骤2：两两合并run，直到只剩1个\n    for len(runs) > 1 {\n        merged := sort1_merge(runs[0], runs[1])\n        runs = append(runs[1:], merged) // 移除前两个，加入合并结果\n        runs = runs[1:]\n    }\n    return runs[0]\n}\n\n// 新增辅助函数：找数组中所有天然有序的子段（核心）\nfunc findRuns(nums []int) [][]int {\n    runs := [][]int{}\n    n := len(nums)\n    if n == 0 {\n        return runs\n    }\n    // 从第一个元素开始，找连续有序的子段\n    start := 0\n    for i := 1; i < n; i++ {\n        // 当当前元素小于前一个，说明有序段结束\n        if nums[i] < nums[i-1] {\n            runs = append(runs, nums[start:i])\n            start = i\n        }\n    }\n    // 加入最后一个有序段\n    runs = append(runs, nums[start:])\n    return runs\n}\n\n// 完全复用原有merge函数（无需修改！）\nfunc sort1_merge(nums1 []int, nums2 []int) []int {\n    if len(nums1) == 0 {\n        return nums2\n    }\n    if len(nums2) == 0 {\n        return nums1\n    }\n    res := make([]int, 0, len(nums1)+len(nums2))\n    i, j := 0, 0\n    for i < len(nums1) && j < len(nums2) {\n        if nums1[i] < nums2[j] {\n            res = append(res, nums1[i])\n            i++\n        } else {\n            res = append(res, nums2[j])\n            j++\n        }\n    }\n    res = append(res, nums1[i:]...)\n    res = append(res, nums2[j:]...)\n    return res\n}\n\n// 移除原有sort1_range函数（无需再拆分到单个元素）","tc":"","sc":"","extra":"","secFullName":"排序算法"},"sa-归并排序（非自然递归）":{"title":"归并排序（非自然递归）","slug":"","d":"m","secName":"sa-sec-13","oneline":"","steps":[],"code":"func sortArray(nums []int) []int {\n    return sort1(nums)\n}\n\n// 非自然归并（单个合并）\nfunc sort1(nums []int) []int {\n    if len(nums) == 0 { // 新增：处理空数组边界\n        return nums\n    }\n    return sort1_range(nums, 0, len(nums)-1)\n}\n\nfunc sort1_range(nums []int, left, right int) []int {\n    // 改动1：修正递归终止条件（核心！）\n    if left == right {\n        return []int{nums[left]} // 区间只有1个元素，直接返回\n    }\n    mid := left + (right - left)/2\n    // 左边、右边\n    l := sort1_range(nums, left, mid)\n    r := sort1_range(nums, mid+1, right)\n    return sort1_merge(l, r)\n}\n\n// 改动2：重写merge函数，实现完整的有序数组合并\nfunc sort1_merge(nums1 []int, nums2 []int) []int {\n    if len(nums1) == 0 {\n        return nums2\n    }\n    if len(nums2) == 0 {\n        return nums1\n    }\n    // 初始化结果切片和双指针\n    res := make([]int, 0, len(nums1)+len(nums2))\n    i, j := 0, 0\n    // 遍历两个数组，选小的元素加入结果\n    for i < len(nums1) && j < len(nums2) {\n        if nums1[i] < nums2[j] {\n            res = append(res, nums1[i])\n            i++\n        } else {\n            res = append(res, nums2[j])\n            j++\n        }\n    }\n    // 拼接剩余元素（其中一个数组已遍历完）\n    res = append(res, nums1[i:]...)\n    res = append(res, nums2[j:]...)\n    return res\n}","tc":"","sc":"","extra":"","secFullName":"排序算法"},"sa-堆排序":{"title":"堆排序","slug":"","d":"m","secName":"sa-sec-13","oneline":"","steps":[],"code":"package main\n\nimport \"fmt\"\n\n// 原地排序\n// 时间复杂度 O(nlogn)\n// 空间复杂度 O(logn) heapfy的递归栈占用\n\n// 堆排序入口\nfunc heapSort(arr []int) {\n    n := len(arr)\n\n    // 1. 构建最大堆（从最后一个非叶子节点开始）：目的构建一个最大堆\n    // [4, 2, 5, 6, 2, 7]\n    // n/2 - 1 是数学推导的结果，原始应该是：((n-1)-1)/2 = 最后一个节点的父节点 = 最后一个非叶子节点的索引\n    // 基于 父节点索引 = (n-1)/2 计算得出的\n    for i := n/2 - 1; i >= 0; i-- {\n       heapify(arr, n, i)\n    }\n\n    // 2. 一个个从堆顶取出元素：目的排序（从堆顶取出最大值和末尾交换再下滤前n-1个数字，只剩最后一个数字）\n    for i := n - 1; i > 0; i-- {\n       // 当前堆顶最大，交换到数组末尾\n       arr[0], arr[i] = arr[i], arr[0]\n       // 调整剩余元素，重新成为最大堆\n       heapify(arr, i, 0)\n    }\n}\n\n// ===================== 核心：堆化（下沉）=====================\n// 将以 i 为根的子树调整为最大堆\nfunc heapify(arr []int, n int, i int) {\n    largest := i     // 最大值先设为根节点\n    left := 2*i + 1  // 左孩子\n    right := 2*i + 2 // 右孩子\n\n    // 如果左孩子更大\n    if left < n && arr[left] > arr[largest] {\n       largest = left\n    }\n    // 如果右孩子更大\n    if right < n && arr[right] > arr[largest] {\n       largest = right\n    }\n\n    // 如果最大值不是根节点，交换 + 继续下沉\n    if largest != i {\n       arr[i], arr[largest] = arr[largest], arr[i]\n       heapify(arr, n, largest) // 递归堆化受影响的子树\n    }\n}\n\n// 迭代版\n// 迭代版堆化（无递归，真正 O(1) 空间）\nfunc heapify(arr []int, n int, i int) {\n    for {\n        largest := i\n        left := 2*i + 1\n        right := 2*i + 2\n\n        if left < n && arr[left] > arr[largest] {\n            largest = left\n        }\n        if right < n && arr[right] > arr[largest] {\n            largest = right\n        }\n\n        if largest == i {\n            break // 不需要下沉了，退出\n        }\n\n        // 交换\n        arr[i], arr[largest] = arr[largest], arr[i]\n        i = largest // 继续往下沉，不用递归\n    }\n}\n\nfunc main() {\n    arr := []int{4, 2, 5, 6, 2, 7}\n    heapSort(arr)\n    fmt.Println(\"排序后：\", arr)\n}","tc":"","sc":"","extra":"","secFullName":"排序算法"},"sa-哲学家问题":{"title":"哲学家问题","slug":"","d":"m","secName":"sa-sec-14","oneline":"","steps":[],"code":"package main\n\nimport (\n        \"fmt\"\n        \"sync\"\n)\n\n// 【新增】5个叉子锁（每个叉子对应一个互斥锁，模拟真实资源抢占）\nvar forks = [5]sync.Mutex{}\n// 【新增】叉子名称（带序号，方便打印）\nvar forkNames = [5]string{\"叉子0\", \"叉子1\", \"叉子2\", \"叉子3\", \"叉子4\"}\n\ntype Player struct {\n        name int\n        // 【新增】预计算左右叉子序号（环形结构）\n        leftForkIdx  int\n        rightForkIdx int\n}\n\nfunc newPlayer(name int) *Player {\n        leftIdx := name - 1   // 哲学家1→左叉子0，2→1...5→4\n        rightIdx := name % 5  // 哲学家1→右叉子1，2→2...5→0\n        return &Player{\n                name:         name,\n                leftForkIdx:  leftIdx,\n                rightForkIdx: rightIdx,\n        }\n}\n\nfunc main() {\n        p1 := newPlayer(1) // 奇数\n        p2 := newPlayer(2) // 偶数\n        p3 := newPlayer(3) // 奇数\n        p4 := newPlayer(4) // 偶数\n        p5 := newPlayer(5) // 奇数\n\n        n := 100\n        wg := &sync.WaitGroup{}\n        players := 5\n\n        // 100 次循环验证无死锁\n        for i := 0; i < n; i++ {\n                wg.Add(players)\n                go p1.wantToEat(wg)\n                go p2.wantToEat(wg)\n                go p3.wantToEat(wg)\n                go p4.wantToEat(wg)\n                go p5.wantToEat(wg)\n        }\n        wg.Wait()\n        fmt.Println(\"所有循环完成，无死锁！\")\n}\n\n// 核心：奇偶分序拿叉子\nfunc (p *Player) wantToEat(wg *sync.WaitGroup) {\n        defer wg.Done()\n\n        // 【核心逻辑】奇偶分序拿叉子\n        if p.name%2 == 0 {\n                // 偶数哲学家：先拿左叉子，再拿右叉子\n                forks[p.leftForkIdx].Lock()\n                p.pLeft()\n                forks[p.rightForkIdx].Lock()\n                p.pRight()\n        } else {\n                // 奇数哲学家：先拿右叉子，再拿左叉子（打破循环等待）\n                forks[p.rightForkIdx].Lock()\n                p.pRight()\n                forks[p.leftForkIdx].Lock()\n                p.pLeft()\n        }\n\n        // 吃饭\n        p.eat()\n\n        // 放叉子：逆序释放（后拿的先放，避免资源混乱）\n        if p.name%2 == 0 {\n                forks[p.rightForkIdx].Unlock()\n                p.vRight()\n                forks[p.leftForkIdx].Unlock()\n                p.vLeft()\n        } else {\n                forks[p.leftForkIdx].Unlock()\n                p.vLeft()\n                forks[p.rightForkIdx].Unlock()\n                p.vRight()\n        }\n}\n\nfunc (p *Player) pLeft() {\n        fmt.Printf(\"哲学家%d 拿到【%s】（左叉子）！\\n\", p.name, forkNames[p.leftForkIdx])\n}\nfunc (p *Player) pRight() {\n        fmt.Printf(\"哲学家%d 拿到【%s】（右叉子）！\\n\", p.name, forkNames[p.rightForkIdx])\n}\nfunc (p *Player) vLeft() {\n        fmt.Printf(\"哲学家%d 放下【%s】（左叉子）！\\n\", p.name, forkNames[p.leftForkIdx])\n}\nfunc (p *Player) vRight() {\n        fmt.Printf(\"哲学家%d 放下【%s】（右叉子）！\\n\", p.name, forkNames[p.rightForkIdx])\n}\nfunc (p *Player) eat() {\n        fmt.Printf(\"===== 哲学家%d 开始吃饭（持有%s+%s）！=====\\n\",\n                p.name, forkNames[p.leftForkIdx], forkNames[p.rightForkIdx])\n}","tc":"","sc":"","extra":"","secFullName":"并发专题"},"sa-LRU缓存":{"title":"LRU缓存","slug":"","d":"m","secName":"sa-sec-15","oneline":"","steps":[],"code":"// list的元素是一个 kv 对的原因：为了 O(1)通过 v 找到 k，从而操作\ntype entry struct {\n    key, value int\n}\n\ntype LRUCache struct {\n    capacity  int\n    list      *list.List // 双向链表\n    keyToNode map[int]*list.Element\n}\n\nfunc Constructor(capacity int) LRUCache {\n    return LRUCache{capacity, list.New(), map[int]*list.Element{}}\n}\n\nfunc (c *LRUCache) Get(key int) int {\n    node := c.keyToNode[key]\n    if node == nil { // 没有这本书\n        return -1\n    }\n    c.list.MoveToFront(node) // 把这本书放在最上面\n    return node.Value.(entry).value\n}\n\nfunc (c *LRUCache) Put(key, value int) {\n    if node := c.keyToNode[key]; node != nil { // 有这本书\n        node.Value = entry{key, value} // 更新\n        c.list.MoveToFront(node) // 把这本书放在最上面\n        return\n    }\n    c.keyToNode[key] = c.list.PushFront(entry{key, value}) // 新书，放在最上面\n    if len(c.keyToNode) > c.capacity { // 书太多了\n        // delete(c.keyToNode, c.list.Remove(c.list.Back()).(entry).key) // 去掉最后一本书\n        deleteKey := c.list.Remove(c.list.Back()).(entry).key\n        delete(c.keyToNode, deleteKey)\n    }\n}","tc":"","sc":"","extra":"","secFullName":"腾讯算法真题"},"sa-合并两个有序列表":{"title":"合并两个有序列表","slug":"","d":"m","secName":"sa-sec-15","oneline":"","steps":[],"code":"func mergeList(l1 *ListNode, l2 *ListNode) *ListNode{\n    if l1 == nil{\n        return l2\n    }\n    if l2 == nil{\n        return l1\n    }\n    if l1.Val < l2.Val{\n        l1.Next = mergeList(l1.Next, l2)\n        return l1\n    }else{\n        l2.Next = mergeList(l1, l2.Next)\n        return l2\n    }\n}","tc":"","sc":"","extra":"","secFullName":"腾讯算法真题"},"sa-合并 K 个有序列表":{"title":"合并 K 个有序列表","slug":"","d":"m","secName":"sa-sec-15","oneline":"","steps":[],"code":"func mergeKLists(lists []*ListNode) *ListNode {\n    if len(lists) == 0 {\n        return nil\n    }\n    // 分治\n    return mergeDive(lists, 0, len(lists)-1)\n}\n\nfunc mergeDive(lists []*ListNode, left, right int) *ListNode{\n    if left == right{\n        // 同一个链表，说明只剩下一个了\n        return lists[left]\n    }\n    mid := left + (right - left)/2\n    // 分别合并左半部分、右半部分\n    l1 := mergeDive(lists, left, mid)\n    l2 := mergeDive(lists, mid+1, right)\n    // 最后合并两部分\n    return mergeList(l1, l2)\n}\n\nfunc mergeList(l1 *ListNode, l2 *ListNode) *ListNode{\n    if l1 == nil{\n        return l2\n    }\n    if l2 == nil{\n        return l1\n    }\n    if l1.Val < l2.Val{\n        l1.Next = mergeList(l1.Next, l2)\n        return l1\n    }else{\n        l2.Next = mergeList(l1, l2.Next)\n        return l2\n    }\n}","tc":"","sc":"","extra":"","secFullName":"腾讯算法真题"},"sa-合并两个链表":{"title":"合并两个链表","slug":"","d":"m","secName":"sa-sec-15","oneline":"","steps":[],"code":"/**\n * Definition for singly-linked list.\n * type ListNode struct {\n *     Val int\n *     Next *ListNode\n * }\n */\nfunc mergeInBetween(list1 *ListNode, a int, b int, list2 *ListNode) *ListNode {\n    // 找 a前驱 b后继\n    l := list1\n    for i:=0;i<a-1;i++{\n        l = l.Next\n    }\n    r := l\n    for i:=0;i<b-a+2;i++{\n        // 从 a-1 到 b+1\n        r = r.Next\n    }\n    // 接list2\n    if list2 == nil{\n        l.Next = r\n        return list1\n    }\n    l.Next = list2\n    end := list2\n    for end.Next != nil{\n        end = end.Next\n    }\n    end.Next = r\n    return list1\n}","tc":"","sc":"","extra":"","secFullName":"腾讯算法真题"},"sa-LFU缓存":{"title":"LFU缓存","slug":"","d":"m","secName":"sa-sec-15","oneline":"","steps":[],"code":"type entry struct {\n    key, value, freq int // freq 表示这本书被看的次数\n}\n\ntype LFUCache struct {\n    capacity   int\n    minFreq    int\n    keyToNode  map[int]*list.Element\n    freqToList map[int]*list.List\n}\n\nfunc Constructor(capacity int) LFUCache {\n    return LFUCache{\n        capacity:   capacity,\n        keyToNode:  map[int]*list.Element{},\n        freqToList: map[int]*list.List{},\n    }\n}\n\nfunc (c *LFUCache) pushFront(e *entry) {\n    if _, ok := c.freqToList[e.freq]; !ok {\n        c.freqToList[e.freq] = list.New() // 双向链表\n    }\n    c.keyToNode[e.key] = c.freqToList[e.freq].PushFront(e)\n}\n\nfunc (c *LFUCache) getEntry(key int) *entry {\n    node := c.keyToNode[key]\n    if node == nil { // 没有这本书\n        return nil\n    }\n    e := node.Value.(*entry)\n    lst := c.freqToList[e.freq]\n    lst.Remove(node) // 把这本书抽出来\n    if lst.Len() == 0 { // 抽出来后，这摞书是空的\n        delete(c.freqToList, e.freq) // 移除空链表\n        if c.minFreq == e.freq { // 这摞书是最左边的\n            c.minFreq++\n        }\n    }\n    e.freq++ // 看书次数 +1\n    c.pushFront(e) // 放在右边这摞书的最上面\n    return e\n}\n\nfunc (c *LFUCache) Get(key int) int {\n    if e := c.getEntry(key); e != nil { // 有这本书\n        return e.value\n    }\n    return -1 // 没有这本书\n}\n\nfunc (c *LFUCache) Put(key, value int) {\n    if e := c.getEntry(key); e != nil { // 有这本书\n        e.value = value // 更新 value\n        return\n    }\n    if len(c.keyToNode) == c.capacity { // 书太多了\n        lst := c.freqToList[c.minFreq] // 最左边那摞书\n        k := lst.Remove(lst.Back()).(*entry).key // 移除最左边那摞书的最下面的书\n        delete(c.keyToNode, k)\n        if lst.Len() == 0 { // 这摞书是空的\n            delete(c.freqToList, c.minFreq) // 移除空链表\n        }\n    }\n    c.pushFront(&entry{key, value, 1}) // 新书放在「看过 1 次」的最上面\n    c.minFreq = 1\n}","tc":"","sc":"","extra":"","secFullName":"腾讯算法真题"},"sa-系统":{"title":"系统","slug":"","d":"m","secName":"sa-sec-17","oneline":"","steps":[],"code":"type TreeNode struct {\n    Val   int\n    Left  *TreeNode\n    Right *TreeNode\n}\n\n// 前序遍历\nfunc preorder(root *TreeNode, res *[]int) {\n    if root == nil {\n        return\n    }\n    *res = append(*res, root.Val) // 处理根\n    preorder(root.Left, res)      // 左子树\n    preorder(root.Right, res)     // 右子树\n}\n\n// 中序遍历\nfunc inorder(root *TreeNode, res *[]int) {\n    if root == nil {\n        return\n    }\n    inorder(root.Left, res)       // 左子树\n    *res = append(*res, root.Val) // 处理根\n    inorder(root.Right, res)      // 右子树\n}\n\n// 后序遍历\nfunc postorder(root *TreeNode, res *[]int) {\n    if root == nil {\n        return\n    }\n    postorder(root.Left, res)     // 左子树\n    postorder(root.Right, res)    // 右子树\n    *res = append(*res, root.Val) // 处理根\n}","tc":"","sc":"","extra":"","secFullName":"树\\-DFS"},"sa-面向问题":{"title":"面向问题","slug":"","d":"m","secName":"sa-sec-18","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"KMP算法"},"sa-kmp是解决什么问题的？":{"title":"kmp是解决什么问题的？","slug":"","d":"m","secName":"sa-sec-18","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"KMP算法"},"sa-kmp解决问题的核心作用？":{"title":"kmp解决问题的核心作用？","slug":"","d":"m","secName":"sa-sec-18","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"KMP算法"},"sa-怎么实现kmp算法？（需要克服哪些问题）":{"title":"怎么实现kmp算法？（需要克服哪些问题）","slug":"","d":"m","secName":"sa-sec-18","oneline":"","steps":[],"code":"","tc":"","sc":"","extra":"","secFullName":"KMP算法"},"sa-代码实现（各种子问题、扩展问题）":{"title":"代码实现（各种子问题、扩展问题）","slug":"","d":"m","secName":"sa-sec-18","oneline":"","steps":[],"code":"// 利用kmp算法原理，借用库函数\n// 移动匹配\n// 只要两个s拼接在一起，移除前后两个字符，如果里面还出现一个s的话，就说明是由重复子串组成。\nfunc repeatedSubstringPattern(s string) bool {\n    if len(s) == 0 {return false}\n    t := s + s\n    return strings.Contains(t[1:len(t)-1], s)\n}","tc":"","sc":"","extra":"","secFullName":"KMP算法"}}};